# 20260705 星网锐捷分析结果优化建议

> 本文基于 2026-07-05 前端输入“星网锐捷”后的本地运行日志复盘。由于本次预测记录保存失败，无法从 `data/predictions.db` 读取完整历史报告，因此以下判断主要依据 `logs/market-prediction-2026-07-05.log`。
>
> 2026-07-05 更新: 第 1-3 天路线已完成第一轮落地。输入 `星网锐捷` 现在会解析为 `002396` / A 股，技术面会生成近 30 个交易日走势，前端也已接入近期走势折线图。

## 1. 本次结果为什么差

这次问题不是单个 Prompt 写得不好，而是“标的识别、依赖环境、数据获取、失败状态、结果保存”连续出问题。

### 1.1 标的识别错误

日志显示:

```text
获取 星网锐捷 (US) 的 K 线数据，周期=3mo
```

“星网锐捷”是 A 股上市公司，代码应解析为 `002396`，市场应为 `A`。当前系统没有把中文 A 股名称解析成代码，非数字输入又被默认当成 `US`，导致行情、新闻、基本面、行业数据链路一开始就走偏。

### 1.2 关键数据依赖缺失

当前 Anaconda 环境缺少:

```text
yfinance
akshare
```

日志中的直接影响:

- 技术面: `No module named 'yfinance'`，K 线获取失败。
- 新闻面: `yfinance 新闻获取异常`，新闻为 0 条，退回知识库分析。
- 基本面: `基本面数据获取异常: No module named 'yfinance'`。
- 宏观面: `LPR/M2 获取失败: No module named 'akshare'`，宏观新鲜度只有 `32%`。
- 地缘事件: `No module named 'akshare'`。

### 1.3 Agent 异常被包装成“成功”

日志里技术面和基本面都出现异常:

```text
近期股价分析师 分析异常: No module named 'yfinance'
公司前景分析师 分析异常: Replacement index 2 out of range for positional args tuple
```

但调度器最后显示:

```text
分析完成 | 成功=5 | 失败=0
```

这说明当前系统会把异常 Agent 包装成低置信度结果返回，Aggregator 又把它们当作“成功参与”。这会让前端看起来五个 Agent 都跑了，但实际有效数据很少。

### 1.4 基本面 Agent 有明确代码 bug

`公司前景分析师` 报错:

```text
Replacement index 2 out of range for positional args tuple
```

位置在 `src/agents/fundamental_analyst.py` 的 `_build_step_a_prompt()`，说明 prompt 字符串格式化占位符和 `.format(...)` 参数数量不匹配。这不是数据源问题，是确定性的代码缺陷。

### 1.5 宏观 Agent 的两步推理也不稳定

日志显示:

```text
两步推理异常('\n  "transmission_chains"')，回退单 pass
```

这通常是 prompt 模板里的 JSON 花括号没有正确转义，或 LLM 返回 JSON 解析逻辑太脆弱。虽然最终有宏观结果，但它已经进入降级路径。

### 1.6 历史保存失败

最终保存预测记录失败:

```text
预测记录失败: 'FinalReport' object has no attribute '_quality_tag'
```

所以这次前端结果没有可靠落库，历史页也无法复盘完整报告。这会严重影响后续调试。

## 2. 最大瓶颈排序

| 优先级 | 问题 | 影响 |
| --- | --- | --- |
| P0 | 中文标的无法解析为 A 股代码 | 所有数据源都可能走错市场 |
| P0 | 当前运行环境缺 `akshare/yfinance` | 行情、新闻、宏观、基本面大面积降级 |
| P0 | 失败 Agent 被算作成功 | Aggregator 误以为信息完整 |
| P0 | `PredictionStore` 保存 `_quality_tag` 报错 | 无法沉淀历史结果 |
| P1 | 基本面 prompt 格式化 bug | 公司前景维度不可用 |
| P1 | 宏观两步 JSON 推理脆弱 | 宏观解释容易降级和泛化 |
| P1 | 新闻/行业缺 A 股专项数据源 | 小盘/中盘 A 股容易出现空数据 |

## 3. 建议先重点优化哪个 Agent

建议从 **近期股价分析师** 开始。

理由:

1. 星网锐捷这类 A 股短期预测，技术面是最基础的锚点。没有 K 线、成交量、均线和波动区间，其他 Agent 很容易变成泛泛而谈。
2. 技术面优化更可验证。只要给定固定 OHLCV fixture，就能稳定测试，不依赖 LLM、新闻网站或财报接口。
3. 它能顺手打通中文标的解析、市场识别、行情数据质量这些全局基础设施。
4. 本次技术面是硬失败，不是“观点质量一般”。修好后对结果提升最直接。

基本面 Agent 也有明确 bug，但建议作为 P0 快速修复，不作为第一阶段“重点优化对象”。先把技术面做到可依赖，再推进基本面深度优化。

## 4. 近期股价分析师优化方案

目标: 输入 `星网锐捷` 时，系统能稳定识别为 `002396` / A 股，并给出基于真实或明确标注数据源的短期技术分析。如果数据不足，应明确拒绝强判断，而不是编出结论。

### 4.1 建立统一标的解析器

新增或抽出统一模块:

```text
src/data/symbol_resolver.py
```

能力:

- 中文名、简称、带后缀代码统一解析。
- `星网锐捷` → `002396`，market=`A`。
- 支持别名缓存，例如 `腾讯` → `0700`，`美团` → `3690`。
- 后续可用 `akshare.stock_info_a_code_name()` 自动补全 A 股名称映射。
- 所有 Agent 和 `api_server.py` 都调用同一个 resolver，避免每个文件各写一套 `_identify_market()`。

验收:

```text
resolve("星网锐捷") => symbol="002396", market="A", name="星网锐捷"
resolve("002396") => symbol="002396", market="A"
resolve("002396.SZ") => symbol="002396", market="A"
```

### 4.2 技术面数据层必须先于 LLM 成功

技术 Agent 的数据质量门槛:

- 至少 60 个交易日 K 线。
- 最新交易日距离当前日期不超过 3 个交易日。
- 必须有 `open/high/low/close/volume`。
- 数据源、最新交易日、样本行数必须写入 `data_summary`。

如果不满足门槛:

- 方向固定为 `neutral`。
- 置信度不超过 `0.2`。
- reasoning 第一段必须说明“数据不足，不能形成有效技术判断”。
- Aggregator 应降低该 Agent 权重，而不是当作正常观点。

### 4.3 A 股行情优先走 A 股数据源

对于 `market=A`:

- 优先 `akshare` A 股历史行情。
- 可增加新浪/东方财富作为备选。
- 不应优先走 `yfinance`。

这能避免中文名被误判成美股后再依赖 `yfinance`。

### 4.4 将技术指标计算拆成纯函数

建议把以下逻辑拆到可测试模块:

```text
src/data/technical_features.py
```

包含:

- 均线趋势: MA5/MA10/MA20/MA60。
- MACD 状态。
- RSI/KDJ 超买超卖。
- 布林带位置。
- 成交量放大/缩量。
- 近 5/10/20 日收益和波动。
- 支撑/压力位。

LLM 只负责解释，不负责“凭空计算指标”。

### 4.5 建立星网锐捷 fixture 测试

新增无网络测试 fixture:

```text
tests/fixtures/price_002396.csv
```

覆盖:

- 中文名解析测试。
- `PriceFetcher.fetch("星网锐捷")` 最终调用 A 股代码。
- 技术指标纯函数测试。
- 技术 Agent 在数据完整时输出非空 `data_summary`。
- 技术 Agent 在数据缺失时降级，不编造强方向。

## 5. 必须顺手修掉的 P0 小问题

即使主线优化技术 Agent，也建议同时处理这些小问题，否则复盘会继续困难。

### 5.1 修复历史保存

当前保存失败:

```text
'FinalReport' object has no attribute '_quality_tag'
```

建议:

- 检查 `FinalReport.to_json()` / `to_dict()` / `to_markdown()` 是否引用了未定义字段。
- 如果质量标签是新增概念，应给 `FinalReport` 明确定义字段或用 `getattr(report, "_quality_tag", None)` 兼容。
- 加一个保存完整报告的单元测试。

### 5.2 失败 Agent 不应算作正常成功

建议在 `AnalysisResult` 或 API 响应里增加:

```text
status: ok | degraded | failed
error_message: Optional[str]
data_quality_score: float
```

Aggregator 权重应基于 `status` 和 `data_quality_score` 调整，而不是只看是否返回了对象。

### 5.3 修复基本面 prompt 格式化

`fundamental_analyst.py` 中 `_build_step_a_prompt()` 的 `.format()` 报错需要马上修。

常见修法:

- f-string 中的 JSON 示例花括号改成 `{{` / `}}`。
- `.format()` 参数改为命名参数。
- 增加一个最小数据输入的 prompt 构造测试，确保不再抛 `IndexError`。

### 5.4 安装并锁定运行依赖

当前 `requirements.txt` 已写入 `akshare/yfinance`，但本机环境没装。

建议:

```powershell
D:\anaconda\python.exe -m pip install -r requirements.txt
```

后续最好使用项目虚拟环境，减少 Anaconda 全局包冲突。

## 6. 推荐实施顺序

### 第 1 天: 修基础链路

1. 安装缺失依赖。
2. 修复 `FinalReport._quality_tag` 保存失败。
3. 修复 `fundamental_analyst.py` prompt 格式化 bug。
4. 新增 `symbol_resolver.py`，至少覆盖星网锐捷、腾讯、美团、平安银行等常用测试样例。

### 第 2 天: 深化技术 Agent

1. A 股行情优先走 AkShare，并增加腾讯 K 线 API 兜底。
2. 技术指标拆成纯函数。
3. 技术 Agent 输出结构化 `data_summary`。
4. 数据不足时明确降级，不生成强预测。

### 第 3 天: 验证与复盘

1. 用 `星网锐捷`、`000001`、`0700`、`AAPL` 做回归。
2. 对比修复前后:
   - 标的是否识别正确。
   - K 线是否获取成功。
   - Agent 置信度是否合理。
   - Aggregator 是否降低降级 Agent 权重。
   - 历史记录是否可完整保存和查看。

## 7. 当前执行结果

本轮已经完成第 1-3 天路线里的核心优化:

| 路线项 | 当前状态 |
| --- | --- |
| 安装缺失依赖 | 已在本机 Anaconda 环境安装 `akshare`、`yfinance` 及 Web 运行依赖 |
| 修复历史保存 | 已补齐 `FinalReport._quality_tag()`，避免保存完整 Markdown 报告时报错 |
| 修复基本面 prompt bug | 已修复 `_build_step_a_prompt()` 参数数量不匹配问题 |
| 统一标的解析 | 已新增 `src/data/symbol_resolver.py`，覆盖 `星网锐捷`、`002396.SZ`、腾讯、美团等样例 |
| A 股行情链路 | `market=A` 先走 AkShare，失败后走腾讯 K 线 API，不再误入美股链路 |
| 技术面数据质量门控 | 已新增数据质量评分；少于 60 个交易日或数据缺字段时技术 Agent 明确降级/失败 |
| 技术面结构化输出 | `data_summary` 已包含 `latest_date`、`data_quality`、`recent_trend`、指标和近期收盘价 |
| Aggregator 权重 | 已根据 `ok/degraded/failed` 和数据质量降低异常 Agent 权重 |
| API 响应 | 已返回 `resolved_target`、`target_info`、`price_trend` |
| 前端走势 | 已新增近期走势卡片，用技术面 `price_trend` 绘制折线图 |

真实行情烟测结果:

```text
PriceFetcher.fetch("星网锐捷", "3mo")
=> symbol=002396
=> market=A
=> trading_days=80
=> latest_date=2026-07-03
=> data_quality.status=ok
=> trend_points=30
=> last_close=23.4
```

项目级验证:

```text
scripts/check.ps1 -Python D:\anaconda\python.exe
=> 235 passed, 2 skipped, 12 deselected

node --check frontend/app.js
=> passed

POST /api/analyze/async（仅保留技术面 Agent）
=> status=completed
=> resolved_target=002396
=> target_info.display_name=星网锐捷(002396)
=> price_trend_points=30
=> 近期股价分析师 status=ok
```

新增/更新的关键文件:

- `src/data/symbol_resolver.py`: 标的解析。
- `src/data/technical_features.py`: 近期走势与数据质量纯函数。
- `src/data/price_fetcher.py`: A 股解析、腾讯 K 线兜底、`recent_trend` 输出。
- `src/agents/technical_analyst.py`: 数据质量门控和结构化摘要。
- `src/agents/aggregator.py`: 异常 Agent 权重调整。
- `api_server.py`: 解析结果与走势数据返回前端。
- `frontend/app.js` / `frontend/index.html` / `frontend/styles.css`: 近期走势展示。
- `tests/test_symbol_resolver.py` / `tests/test_technical_agent_star_net.py`: 标的解析、A 股行情兜底、走势和降级测试。

## 8. 第一阶段验收标准

输入 `星网锐捷` 后，应满足:

- 前端显示解析后的标的: `星网锐捷 / 002396 / A股`。
- 技术面 Agent 不再出现 `US` 市场识别。
- 技术面数据源显示真实来源和最新交易日。
- 若行情获取失败，前端明确显示失败原因，不输出看似确定的技术判断。
- 历史记录保存成功，历史详情可打开完整报告。
- 默认测试仍通过。

建议新增测试:

```powershell
D:\anaconda\python.exe -m pytest tests/test_symbol_resolver.py tests/test_technical_agent_star_net.py
```

## 9. 一句话结论

这次“星网锐捷”结果差，根因是输入中文 A 股名后系统没有正确解析成 `002396`，进而触发错误市场、缺失依赖、数据降级、异常 Agent 被误当成功和历史保存失败。下一步最值得重点优化的是 **近期股价分析师**，但要把它当成“标的解析 + 行情数据 + 技术指标 + 质量门控”的完整竖切来做，而不是只改 Prompt。

## 10. 近期股价分析师下一轮优化建议

当前第一轮已经解决“能不能取到星网锐捷真实 K 线、能不能在前端看到近期走势、数据不足时能不能明确降级”的基础问题。下一轮优化重点应从“取数可用”推进到“技术判断可信”: 让近期股价分析师先用确定性规则形成结构化证据，再让 LLM 负责解释这些证据，而不是让 LLM 直接凭一串指标自由发挥。

### 10.1 优化目标

近期股价分析师下一轮的目标建议设为:

1. 对 `星网锐捷` 这类 A 股标的，输出能被追溯的短期技术判断。
2. 每一个方向判断都能对应到明确证据: 趋势、量能、波动、位置、支撑压力、风险触发位。
3. 置信度不是 LLM 主观给分，而是由数据质量、信号一致性和历史回测表现共同约束。
4. 前端不仅展示走势，还能展示“为什么这么判断”的关键技术证据。

### 10.2 建议新增 `TechnicalSnapshot`

建议把技术面 Agent 的输入从松散 `dict` 升级为结构化快照，放在 `src/data/technical_features.py` 或新文件 `src/data/technical_snapshot.py`。

建议字段:

```text
TechnicalSnapshot
- symbol / name / market
- latest_date
- trading_days
- data_quality
- close_series_summary
- trend_regime
- momentum_signals
- volume_signals
- volatility_signals
- support_resistance
- risk_levels
- recent_trend
```

其中 `trend_regime` 不要只给 `up/down`，建议拆成:

```text
trend_regime:
  short_term: up | down | sideways
  medium_term: up | down | sideways
  ma_alignment: bullish | bearish | tangled
  slope_score: -1.0 ~ 1.0
  structure: higher_high | lower_low | range_bound | unknown
```

这样 LLM 拿到的不是一堆原始指标，而是一份已经计算过的“技术证据包”。

### 10.3 技术指标优先级

不要一口气堆很多指标。建议优先补这几类，和短期判断最相关。

| 优先级 | 模块 | 要解决的问题 |
| --- | --- | --- |
| P0 | 趋势结构 | 当前是上升、下降还是箱体震荡 |
| P0 | 量价关系 | 上涨是否放量、下跌是否缩量、是否异常放量 |
| P0 | 支撑压力 | 当前价格离关键压力/支撑有多远 |
| P0 | 波动与风险 | 短期振幅、回撤、ATR 风险空间 |
| P1 | 多周期共振 | 日线和周线是否同向 |
| P1 | 大盘/行业相对强弱 | 个股走势是否跑赢中证/深证/行业指数 |
| P2 | 形态识别 | 突破、回踩、平台、跳空、长上影/长下影 |

第一轮不要追求“指标大全”，而是把 P0 做到可解释、可测试、可回测。

### 10.4 量价关系建议

星网锐捷这种中小盘 A 股，短期分析里量能很关键。建议新增:

```text
volume_signals:
  volume_ratio_5d: 当前成交量 / 5日均量
  volume_ratio_20d: 当前成交量 / 20日均量
  price_up_volume_up: bool
  price_down_volume_up: bool
  abnormal_volume: bool
  volume_trend: expanding | shrinking | neutral
```

解释规则:

- 放量上涨: 支持短期延续，但要看是否接近压力位。
- 放量下跌: 风险权重提高，不能只因为超跌就看反弹。
- 缩量横盘: 更适合判断为震荡，不应给高置信方向。
- 异常放量但价格不涨: 需要提示分歧或抛压。

### 10.5 支撑压力与触发位

目前只给走势曲线还不够。技术 Agent 应输出明确的关键价位:

```text
support_resistance:
  nearest_support: 22.80
  nearest_resistance: 24.10
  support_distance_pct: -2.6
  resistance_distance_pct: 3.0
  stop_loss_reference: 22.50
  breakout_reference: 24.20
```

计算建议:

1. 先用近 20/60 日高低点作为基础支撑压力。
2. 再加入 MA20、MA60 作为动态支撑压力。
3. 如果成交密集区暂时没有数据源，可以先不做筹码分布，避免伪精确。
4. 前端可以在趋势图下方展示“最近支撑/最近压力/突破触发/风险位”。

### 10.6 置信度校准

建议近期股价分析师不要让 LLM 直接决定最终置信度。可以先计算一个基础分:

```text
technical_confidence =
  data_quality_score * 0.35
  + signal_consistency_score * 0.35
  + risk_reward_score * 0.15
  + backtest_prior_score * 0.15
```

其中:

- `data_quality_score`: 数据完整性、新鲜度、样本长度。
- `signal_consistency_score`: 趋势、量能、动量、均线是否同向。
- `risk_reward_score`: 距支撑和压力的空间是否合理。
- `backtest_prior_score`: 同类信号在历史回放中的命中表现；没有样本时给中性分，不要给高分。

建议硬限制:

- 数据质量 `failed`: 置信度不超过 `0.2`。
- 趋势和量能互相冲突: 置信度不超过 `0.45`。
- 价格位于压力位下方 2% 内但没有放量突破: 不允许给高置信看涨。
- 只有单一指标支持方向: 置信度不超过 `0.4`。

### 10.7 Prompt 改造

近期股价分析师的 Prompt 应从“请分析这些指标”改成“请基于证据包写结论”。建议传给 LLM 的内容固定为三段:

```text
1. 数据质量:
   latest_date / trading_days / quality_status / issues

2. 技术证据:
   trend_regime / momentum_signals / volume_signals / support_resistance / risk_levels

3. 输出约束:
   - 不得编造未提供的价格、成交量或指标
   - 若证据冲突，必须输出 neutral 或低置信度
   - reasoning 第一段必须说明主导信号
   - risks 必须包含至少一个失效条件
```

输出 JSON 建议增加:

```text
evidence:
  bullish:
    - ...
  bearish:
    - ...
  neutral:
    - ...
invalidation:
  bullish_invalid_below: ...
  bearish_invalid_above: ...
```

这样前端未来可以把“支持看涨/支持看跌/中性因素”分开展示，避免只有一大段自然语言。

### 10.8 前端展示建议

近期走势卡片下一步可以从“折线图”升级为“走势 + 技术证据摘要”:

```text
近期走势
- 30日涨跌幅
- 最新收盘价
- 最近支撑 / 最近压力
- MA20 / MA60 状态
- 量能状态
- 数据最新日期
```

建议视觉上保持克制，不要把图做成交易软件。这个项目是 Agent 分析工具，前端重点应是让用户快速判断“这个结论有没有依据”。

### 10.9 测试与验收

下一轮建议至少新增这些测试:

```text
tests/test_technical_features_v2.py
tests/test_technical_confidence.py
tests/test_technical_prompt_contract.py
```

验收标准:

1. 给定一段明显上升趋势 + 放量数据，`trend_regime.short_term=up`，方向允许偏 bullish。
2. 给定横盘缩量数据，方向必须是 neutral 或低置信度。
3. 给定下跌放量数据，不能输出高置信 bullish。
4. 数据少于 60 个交易日，继续触发降级。
5. Prompt 构造不依赖真实网络和 LLM，单元测试可稳定运行。
6. API 返回的 `data_summary` 中能看到趋势、量能、支撑压力、置信度约束来源。

### 10.10 推荐落地顺序

建议下一轮按这个顺序做:

1. 先实现 `TechnicalSnapshot` 和 P0 技术证据计算，不动 LLM。
2. 给星网锐捷构造 3 组离线 fixture: 上升、震荡、下跌。
3. 加入置信度校准函数，替代 LLM 自由给分。
4. 改造 Prompt，只允许 LLM 基于结构化证据解释。
5. 把支撑压力、量能状态展示到前端走势卡片。
6. 最后再跑真实 `星网锐捷`、`平安银行`、`贵州茅台`、`腾讯` 做回归。

### 10.11 本阶段不建议做的事

- 不建议马上接入复杂筹码分布或 Level2 数据，数据源不稳定会拖慢主线。
- 不建议让 LLM 自己判断“金叉、背离、突破”，这些应由代码计算。
- 不建议用单次真实预测结果评价 Agent 好坏，应使用离线 fixture 和历史回放。
- 不建议把技术 Agent 做成交易信号系统；它应该服务于多 Agent 综合分析，而不是单独给买卖点。

下一步真正值得做的是: **把近期股价分析师从“LLM 解释指标”升级为“规则生成证据，LLM 解释证据”**。这样星网锐捷这类结果才会从“看起来有分析”变成“每个判断都有来源、有边界、有失效条件”。

### 10.12 已实现记录

2026-07-05 已完成上述建议的第一期实现，范围聚焦 P0 技术证据和前端可见性:

| 模块 | 实现内容 |
| --- | --- |
| `src/data/technical_features.py` | 新增 `TechnicalSnapshot` 和 `build_technical_snapshot()`，由代码计算趋势结构、动量、量能、波动、支撑压力、风险位、证据列表和置信度模型 |
| `src/data/price_fetcher.py` | `PriceData` 新增 `technical_snapshot`，每次行情取数后随 `recent_trend` 一起输出 |
| `src/agents/technical_analyst.py` | Prompt 新增“技术证据包”和硬性约束；LLM 结果会被 `confidence_model`、压力位、支撑位和信号一致性约束 |
| `frontend/app.js` / `frontend/index.html` / `frontend/styles.css` | 近期走势卡片新增趋势、量能、支撑、压力、风险位、突破位、证据置信和多空证据摘要 |
| `tests/test_technical_features_v2.py` | 新增上升趋势、横盘缩量、下跌放量三类离线 fixture 测试 |
| `tests/test_technical_agent_star_net.py` | 验证星网锐捷链路会携带 `technical_snapshot`，且 Prompt 遵守“基于证据包解释”的契约 |

当前新增的技术证据字段:

```text
technical_snapshot:
  trend_regime:
    short_term / medium_term / ma_alignment / slope_score / structure
  momentum_signals:
    state / score / macd_signal / rsi / kdj_signal
  volume_signals:
    volume_ratio_5d / volume_ratio_20d / volume_trend
    price_up_volume_up / price_down_volume_up / abnormal_volume
  volatility_signals:
    atr / atr_pct / daily_volatility_20d_pct / max_drawdown_20d_pct
  support_resistance:
    nearest_support / nearest_resistance
    support_distance_pct / resistance_distance_pct
  risk_levels:
    stop_loss_reference / breakout_reference
    downside_to_support_pct / upside_to_resistance_pct / risk_reward_ratio
  confidence_model:
    technical_confidence / signal_consistency_score / risk_reward_score
    suggested_direction / hard_caps
  evidence:
    bullish / bearish / neutral
```

新增验证结果:

```text
D:\anaconda\python.exe -m pytest tests/test_technical_features_v2.py tests/test_technical_agent_star_net.py
=> 10 passed

scripts/check.ps1 -Python D:\anaconda\python.exe
=> 239 passed, 2 skipped, 12 deselected

node --check frontend/app.js
=> passed

POST /api/analyze/async（仅保留近期股价分析师）
=> status=completed
=> resolved_target=002396
=> price_trend_points=30
=> has_technical_snapshot=true
=> technical_confidence=0.872
=> suggested_direction=neutral
=> nearest_support=21.36
=> nearest_resistance=25.82
=> volume_trend=neutral
```

### 10.13 分钟级走势与新鲜度修正

用户反馈指出两个新问题:

1. 近期走势只展示日线，粒度太粗；证券 App 通常会展示分钟线或分时线。
2. 数据新鲜度显示 `2026-07-03`，但当前日期是 `2026-07-05`，前端没有解释这是周末休市导致的正常现象。

本轮已完成修正:

| 模块 | 实现内容 |
| --- | --- |
| `src/data/price_fetcher.py` | `PriceData` 新增 `intraday_trend`、`intraday_meta`、`freshness`；A 股分钟线优先尝试 AkShare，失败后使用腾讯 `ifzq.gtimg.cn` 5 分钟 K 线，再失败时回退腾讯分时接口 |
| `src/data/technical_features.py` | 新增 `describe_market_freshness()`，区分 `fresh`、`recent`、`market_closed`、`intraday`、`intraday_market_closed` 等状态 |
| `src/agents/technical_analyst.py` | `data_summary.freshness` 改为可读说明；同时返回 `intraday_trend` 和 `intraday_meta` |
| `api_server.py` | API 响应新增 `intraday_trend` 和 `intraday_meta` |
| `frontend/app.js` / `frontend/index.html` / `frontend/styles.css` | 近期走势卡片新增“分钟 / 日线”切换；分钟线可用时默认显示分钟走势，不可用时自动回退日线 |
| `tests/test_technical_agent_star_net.py` | 新增周末休市新鲜度测试，并验证分钟走势能随 `PriceFetcher` 输出 |

当前星网锐捷真实验证:

```text
PriceFetcher.fetch("星网锐捷", "3mo")
=> latest_date=2026-07-03
=> intraday_points=96
=> intraday_meta.source=tencent_minute
=> intraday_meta.interval=5m
=> intraday_meta.latest_time=2026-07-03 15:00
=> freshness_note=分钟级走势最新到 2026-07-03 15:00；当前为周末/休市日，A股日线最新交易日为 2026-07-03

POST /api/analyze/async（仅保留近期股价分析师）
=> resolved_target=002396
=> daily_points=30
=> intraday_points=96
=> intraday_meta.latest_time=2026-07-03 15:00
=> freshness=分钟级走势最新到 2026-07-03 15:00；当前为周末/休市日，A股日线最新交易日为 2026-07-03

scripts/check.ps1 -Python D:\anaconda\python.exe
=> 240 passed, 2 skipped, 12 deselected
```

需要注意:

- 当前实现是 5 分钟级走势，不是逐笔成交或 Level2。要做到同花顺/券商 App 那种完整盘口、逐笔、分价量，需要更稳定的实时行情源或付费行情接口。
- 2026-07-05 是周日，A 股没有新的交易分钟线。此时最新可用分钟线停在 2026-07-03 15:00 是合理结果，不代表数据源坏了。
- 技术面 Agent 的核心判断仍以日线技术证据为主，分钟线主要用于前端展示和新鲜度增强，避免盘中噪声过度影响多 Agent 综合判断。

### 10.14 前端走势坐标轴优化

用户反馈近期走势图横轴和纵轴数字重叠。本轮已完成图表细化:

- 图表左侧和底部预留更大的坐标轴空间。
- 纵轴改为 5 档价格刻度，并加入横向虚线网格。
- 横轴按数据长度抽样显示 4-5 个时间点，分钟线跨日时显示 `MM-DD HH:mm`，同日时只显示 `HH:mm`。
- 最新价格点增加圆点和价格标记。
- 每个走势点增加浏览器 hover 提示，显示时间、收盘价和区间涨跌幅。
- 图表高度从紧凑模式提升到更适合阅读的比例，避免标签挤压。

### 10.15 盘中信号入证据包与关键位可视化

继续观察“近期股价分析师”后，仍有一个比较关键的问题: 分钟级走势已经能在前端展示，但它还没有充分进入分析师的判断链路。也就是说，用户能看到更细的线，Agent 却仍主要依赖日线证据。这会导致一种错位: 日线结构可能偏强，但最近交易日尾盘已经明显走弱时，结论仍可能给得过于乐观。

建议继续优化三个方向:

1. **分钟线要从展示数据升级为分析证据**
   把 `intraday_trend` 压缩成 `intraday_signals`，输出盘中状态、涨跌幅、VWAP 偏离、从高点回落、从低点反弹、尾盘动量和多空证据。LLM 只负责解释这些证据，不直接凭分钟点位自由发挥。

2. **日线结论和盘中结论冲突时必须降置信**
   如果日线证据偏多，但分钟线已经是明显 `selloff`，看涨置信度不能继续维持高位；反过来，如果日线偏空但分钟线 `strong_up`，看跌置信度也要降级。这样可以减少“图上很弱，文字却很乐观”的问题。

3. **关键支撑/压力要画到走势图上**
   只在下方文字里写“支撑/压力”不够直观。前端走势图应直接叠加最近支撑线和压力线，让用户看到当前价格和关键位置的距离。

本轮已实现:

| 模块 | 实现内容 |
| --- | --- |
| `src/data/technical_features.py` | 新增 `build_intraday_signals()`，将分钟线压缩为盘中状态、VWAP 偏离、回落/反弹、尾盘动量和多空证据 |
| `src/data/price_fetcher.py` | `PriceData` 新增 `intraday_signals`，并通过 `to_agent_dict()` 传给 Agent |
| `src/agents/technical_analyst.py` | Prompt 的技术证据包新增 `intraday_signals`；结果摘要返回该字段；日线方向和盘中方向冲突时自动降低置信度 |
| `frontend/app.js` / `frontend/styles.css` | 走势图新增支撑/压力参考线；证据卡新增“盘中信号”，并补充盘中多空证据摘要 |
| `tests/test_technical_agent_star_net.py` | 新增分钟线强势识别、`PriceFetcher` 输出、Prompt 合同和盘中弱势降置信测试 |

后续还值得继续做的优化:

- 引入交易日历，明确区分“休市导致最新数据停在上一交易日”和“数据源真的落后”。
- 增加指数/行业相对强弱，例如星网锐捷相对深成指、计算机设备行业的 5 日/20 日超额收益。
- 对分钟线做分层展示: 价格线、均价线、成交量柱、关键成交密集区，而不是只画单条收盘线。
- 对 `intraday_signals` 做历史回测，校准 `strong_up`、`selloff`、`mixed` 对未来 1-5 个交易日的真实预测价值。
