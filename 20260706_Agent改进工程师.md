# 20260706 Agent 改进工程师

## 1. 项目双链路划分

项目现在拆成两个相互配合但职责不同的部分:

| 链路 | 目标 | 主要成员 | 输出 |
| --- | --- | --- | --- |
| 股票预测链路 | 对标的未来涨跌做多维预测 | 近期股价、最新新闻、公司前景、行业对比、国际形势、汇总分析师 | 预测方向、幅度、置信度、证据包 |
| Agent 调优链路 | 用真实历史样本评估并改进预测链路 | Agent 改进工程师 | 历史评估报告、错误策略信号、prompt/skill 改进、人工确认建议 |

## 2. Agent 改进工程师定位

`Agent 改进工程师` 不参与股票方向预测。它消费历史评估报告，判断哪些 agent、证据桶、prompt 规则、skill 规则、MCP 或数据源策略在历史样本里有效或错误。

它使用和其他 agent 相同的 LLM API key，但默认改进动作是确定性的；只有打开 LLM 复核时，才调用 LLM 对权限边界和过拟合风险做审查。

## 3. 权限边界

| 类型 | 权限 | 落点 |
| --- | --- | --- |
| prompt guardrail | 可自动写入 | `config/agent_improvement/prompt_guardrails/` |
| 声明式 skill | 可自动新增/修改 | `config/agent_improvement/skills/` |
| 核心 Python 代码 | 只提建议 | `output/agent_improvement_engineer/.../drafts/` |
| MCP 接入 | 只提建议 | `output/agent_improvement_engineer/.../drafts/` |
| 数据源增删改 | 只提建议 | `output/agent_improvement_engineer/.../drafts/` |
| 校准器代码 | 只提建议 | `output/agent_improvement_engineer/.../drafts/` |

声明式 skill 不是可执行代码，而是历史失败场景下的证据检查规则、反例规则和置信度降级规则。

自动写入还需要同时满足两个证据门槛:

- `min_samples_for_auto_apply`: 总历史样本数达到阈值。
- `min_unique_cases_for_auto_apply`: 独立历史案例数达到阈值，避免一两家公司或一两个时间点重复触发导致过拟合。

## 4. 当前已实现

- `src/agents/improvement_engineer.py`
  - 读取历史评估报告。
  - 按 agent 和改进面生成改进草案。
  - 自动写入 prompt guardrail 和声明式 skill。
  - 对 data_source / MCP / calibration / core_code 只生成人工确认建议。
  - 输出 JSON/Markdown 执行报告。
  - 只对 `wrong_strategy` 信号生成自动动作；`strength` 信号只作为保留优势参考。

- `src/prompts/dynamic_overrides.py`
  - 在预测 agent 运行时动态加载已应用的 prompt guardrail 和声明式 skill。
  - 五个预测 agent 与 Aggregator 已接入动态附录。

- `src/core/self_improvement_lab.py`
  - 主动用真实历史 K 线构造技术面历史样本。
  - 可接收新闻快照归档文件，对新闻面做历史回放。
  - 可接收 point-in-time 快照，对公司前景、行业对比、国际形势做历史回放。
  - 统一生成历史评估报告，并可继续调用 Agent 改进工程师。
  - 没有快照时明确列为待补，而不是用当前数据伪造过去。

- `src/data/point_in_time_snapshot_archive.py`
  - 新增基本面/行业/宏观通用快照归档器。
  - 支持目录、JSON、JSONL 读取。
  - 标准快照字段包括 `agent_name`、`target`、`symbol`、`market`、`as_of`、`valid_date`、`data`、`stock_context`。

- `scripts/run_improvement_engineer.py`
  - 命令行运行 Agent 改进工程师。

- `scripts/run_self_improvement_lab.py`
  - 命令行运行主动历史样本池。

- `scripts/archive_point_in_time_snapshots.py`
  - 从现在开始归档公司前景、行业对比、国际形势的当前可见数据。
  - 这些快照到期后可被主动历史样本池用于真实验证。

- Web API
  - `GET /api/improvement/status`
  - `POST /api/improvement/evaluate`
  - `POST /api/improvement/run`
  - `POST /api/improvement/self-bootstrap`

- 前端
  - 新增“调优”页面。
  - 可生成历史评估。
  - 可运行 Agent 改进工程师。
  - 可运行主动历史样本池。
  - 可查看自动动作、演练动作和需人工确认建议。

## 5. 推荐使用流程

优先流程:

```text
主动选择股票池和历史区间 -> 构造真实历史样本 -> 历史评估 -> Agent 改进工程师 -> prompt/skill 自动改进 + 高风险建议 -> 再回放验证
```

补充流程:

```text
真实预测 -> 到期验证 -> 写入 predictions.db -> 历史评估 -> 补充在线验证证据
```

默认建议先使用 dry-run:

```bash
python scripts/archive_point_in_time_snapshots.py --targets 000001,600519,AAPL
python scripts/run_self_improvement_lab.py --targets 000001,600519,AAPL --start 2025-01-01 --end 2025-12-31 --run-engineer --dry-run
python scripts/evaluate_historical_samples.py --prediction-db data/predictions.db
python scripts/run_improvement_engineer.py --evaluation-report output/historical_agent_evaluation_xxx.json --dry-run --min-unique-cases 5
```

确认报告合理后，再允许 prompt/声明式 skill 自动落地。

## 6. 当前边界

主动历史样本池当前已经能可靠覆盖:

- 近期股价分析师: 直接用历史 K 线生成当时点技术面样本。
- 最新新闻分析师: 需要已有新闻快照归档文件，才能保证不使用未来新闻。
- 公司前景分析师: 需要 point-in-time 财报、估值、经营指标快照。
- 行业对比分析师: 需要 point-in-time 行业成分股、行业均值、ROE/PE/PB 排名快照。
- 国际形势分析师: 需要 point-in-time 宏观指标、汇率、利率、地缘风险和标的敏感度快照。

已实现的使用方式:

```bash
python scripts/run_self_improvement_lab.py \
  --targets 000001,600519,AAPL \
  --start 2025-01-01 \
  --end 2025-12-31 \
  --news-snapshots data/news_snapshots \
  --point-in-time-snapshots data/point_in_time_snapshots \
  --run-engineer \
  --dry-run
```

这些不能用当前最新数据倒灌回过去，否则会产生未来数据泄漏。
