# 20260706 公司前景分析师优化建议

> 本文接续 `20260705_星网锐捷优化建议.md` 的复盘方法：先把数据链路和代码计算证据做扎实，再让 LLM 基于证据解释，而不是让 LLM 自由发挥。2026-07-05 的近期股价分析师已经完成“结构化技术证据包 + 数据质量门控 + Prompt 契约 + 前端可见性”的第一期优化。公司前景分析师今天也应按同一方向推进。

## 1. 当前状态

公司前景分析师已经不是早期简单 Prompt。当前代码已有:

- 基本面数据获取: A 股、港股、美股多市场数据链路。
- 预处理管线: 财务趋势、质量评分卡、数据质量评估、价值陷阱检测。
- 估值分位: A 股可通过 PE 历史序列计算 3 年分位。
- 两步推理: Step A 做公司质量/估值/风险预评估，Step B 做最终判断和反思。
- 专用校准器: `FundamentalConfidenceCalibrator` 支持按置信度桶、数据质量、评分卡等级回归。

这说明它已经有一套不错的“基本面研究框架”。当前主要问题不是功能少，而是 **证据、校验、汇总层之间没有完全闭环**。

## 2. 当前主要问题

### P0: 一致性校验存在确定性 bug

`src/agents/fundamental_analyst.py` 的 `_validate_consistency()` 使用了未定义变量 `pct`。当出现“高 PE 分位但方向看涨”或“低 PE 分位但方向看跌”时，校验分支会抛 `NameError`，导致 Agent 在最后阶段失败。

### P0: 校验问题只写日志，汇总层看不见

此前 `_validate_consistency()` 只 `logger.warning()`，不返回问题，也不写入 `AnalysisResult.risks/status/data_summary`。这会导致:

- 前端看不到基本面判断哪里自相矛盾。
- Aggregator 无法根据校验问题降低权重。
- 历史记录无法复盘“为什么这个结果可信度被怀疑”。

### P0: `data_summary` 缺少基本面结构化证据

公司前景分析师虽然输入里有评分卡、估值分析、价值陷阱和数据质量，但最终 `AnalysisResult.data_summary` 没有稳定输出这些字段。结果是 API、前端和 Aggregator 只能读 reasoning 文本，和昨天技术面优化前的问题类似。

### P1: 置信度上限可能被历史校准器重新抬高

当前 `_calibrate_confidence()` 先按本次数据质量做 ceiling 限制，再调用 `FundamentalConfidenceCalibrator`。如果未来历史样本足够，校准器可能把置信度上调，最终超过本次数据质量上限。正确顺序应是: 历史校准可以参与，但最终仍必须服从本次数据质量 ceiling。

### P1: Step A JSON 解析失败时 Step B 会退化

如果 Step A 返回 JSON 解析失败，Step B 只能看到大量 `N/A`。但我们已经有代码计算出的评分卡、估值分位、价值陷阱和数据质量，应提供一个保守的代码 fallback，而不是把 Step A 空结果直接传下去。

### P1: 短期预测里基本面容易过度表达

基本面通常更适合中长期，短期只有在财报季、业绩预告、重大评级调整等催化剂出现时才应强影响方向。当前 Prompt 有提醒，但还缺少代码层面的硬约束和结构化催化剂字段。

## 3. 本轮优化目标

本轮不追求“基本面指标大全”，而是先完成和技术面类似的第一阶段闭环:

1. 修复确定性 bug，确保一致性校验不会让 Agent 崩溃。
2. 建立基本面证据包，让 Step B 和最终摘要都使用代码计算证据。
3. 让一致性校验结果进入 `risks/reasoning/status/data_summary`。
4. 确保置信度最终服从本次数据质量上限。
5. Step A 解析失败时，用评分卡/估值/价值陷阱生成保守预判。
6. 增加无网络单元测试，覆盖 bug 和护栏行为。

## 4. 建议的基本面证据包

建议公司前景分析师长期维护一个结构化证据包，字段如下:

```text
fundamental_evidence:
  identity:
    symbol / name / market / industry / data_source
  quality_scorecard:
    total / rating / profitability / growth / valuation / health
  valuation_analysis:
    current_pe / pe_percentile_3yr / interpretation / data_points
  financial_trend:
    revenue_trend / profit_trend / margin_trend / roe_trend / earnings_quality
  value_trap_analysis:
    is_trap / risk_level / signals
  data_quality:
    overall_quality / confidence_ceiling / data_gaps
  anomaly_flags:
    value_trap_warning / earnings_momentum_positive / data_heavily_missing
  confidence_constraints:
    ceiling / quality_bucket
```

这些字段应同时进入:

- Step B Prompt，约束 LLM 基于证据判断。
- `AnalysisResult.data_summary`，供 API、前端、Aggregator 和历史记录读取。

## 5. 一致性护栏建议

建议至少保留以下硬护栏:

| 场景 | 处理 |
| --- | --- |
| PE 分位 > 85% 但方向 bullish | 写入风险，降低置信度，标记 degraded |
| PE 分位 < 15% 但方向 bearish | 写入风险，降低置信度，标记 degraded |
| 触发价值陷阱但方向 bullish | 写入风险，降低置信度，标记 degraded |
| 数据完整度 < 40% 但非中性且置信度 > 40% | 写入风险，降低置信度，标记 degraded |
| confidence 超过数据质量 ceiling | 最终强制压回 ceiling |
| 看涨但没有任何风险提示 | 写入风险并降低结果质量 |

注意: 护栏不是替代 LLM，而是防止 LLM 把结构化证据讲反。

## 6. 本轮已执行优化

本轮已经完成以下修改:

| 文件 | 修改 |
| --- | --- |
| `src/agents/fundamental_analyst.py` | 修复 `_validate_consistency()` 中 `pct` 未定义 bug |
| `src/agents/fundamental_analyst.py` | `_validate_consistency()` 改为返回问题列表 |
| `src/agents/fundamental_analyst.py` | 新增 `_apply_consistency_issues()`，把校验问题写入 `risks/reasoning/status` 并降低置信度 |
| `src/agents/fundamental_analyst.py` | 新增 `_build_evidence_packet()`，给 Step B 注入代码计算的基本面证据包 |
| `src/agents/fundamental_analyst.py` | 新增 `_build_data_summary()`，把评分卡、估值、趋势、价值陷阱、数据质量和校验问题输出给汇总层 |
| `src/agents/fundamental_analyst.py` | 新增 `_fallback_step_a_result()`，Step A JSON 解析失败时生成保守预判 |
| `src/agents/fundamental_analyst.py` | 历史校准后再次强制应用本次数据质量 ceiling |
| `tests/test_fundamental_v3.py` | 新增公司前景分析师护栏测试，覆盖高估值看涨、校验降权、结构化摘要 |

## 7. 验证结果

本轮已运行:

```bash
pytest -q tests/test_fundamental_v3.py tests/test_fundamental_v2.py
```

结果:

```text
67 passed
```

## 8. 下一轮建议

下一轮建议继续做“基本面证据 → 验证闭环”:

1. 给公司前景分析师增加离线 fixture，覆盖好公司低估、差公司低估、好公司高估、数据严重缺失四类场景。
2. 把基本面结果在前端展示成“质量评分 / 估值分位 / 数据完整度 / 价值陷阱”四个清晰指标。
3. 为港股和美股补更可靠的历史估值分位，避免 A 股有分位、港美股只能靠当前 PE。
4. 增加现金流/扣非净利/应收账款等盈利质量字段，减少只看利润同比的误判。
5. 在预测验证完成后，把基本面 agent 的实际表现写回 `FundamentalConfidenceCalibrator`。
6. 建立基本面历史快照回放，避免回测时使用未来财务数据。

## 9. 一句话结论

公司前景分析师已经具备评分卡、估值分位和价值陷阱这些关键模块。今天最重要的优化不是继续堆 Prompt，而是把这些代码证据变成最终结果的硬约束和可见摘要。只有这样，Aggregator 才能知道哪些基本面结论可靠、哪些只是低质量数据下的保守推断。

## 10. 第二轮执行记录：基本面证据 → 验证闭环

按照“基本面证据 → 验证闭环”的方向，本轮继续完成了以下优化:

| 文件 | 修改 |
| --- | --- |
| `src/agents/fundamental_analyst.py` | 新增基本面质量-估值决策矩阵，输出好公司/一般公司/弱公司与低估/合理/高估组合判断 |
| `src/agents/fundamental_analyst.py` | 基本面证据包新增 bullish/bearish/neutral 三类证据列表，便于 Prompt、API 和汇总层解释结论 |
| `src/agents/fundamental_analyst.py` | 新增基本面证据硬约束，方向与代码矩阵冲突时写入风险、标记 degraded，并限制 confidence |
| `src/agents/fundamental_analyst.py` | 数据严重缺失、缺少历史估值分位、短期缺少明确催化剂时，自动生成 confidence 上限 |
| `src/data/prediction_store.py` | 预测验证完成后，读取 `agent_results` 中的公司前景分析师样本并回写历史校准器 |
| `src/utils/fundamental_calibrator.py` | 新增公开 `save()` 方法，支持验证闭环显式持久化校准统计 |
| `tests/test_fundamental_v3.py` | 新增好公司低估、弱公司低估且价值陷阱、好公司高估、数据严重缺失、矩阵冲突降权五类离线测试 |
| `tests/test_phase3.py` | 新增预测验证回写公司前景校准器测试 |

本轮的关键变化是: 公司前景分析师不再只把财务证据交给 LLM 自由解释，而是先由代码生成可验证的基本面矩阵、证据列表和置信度上限，再让 LLM 在这些约束内组织最终分析。

已运行:

```bash
pytest -q tests/test_fundamental_v3.py tests/test_fundamental_v2.py tests/test_phase3.py
```

结果:

```text
90 passed
```

## 11. 再下一轮优化建议

再下一轮建议继续沿着“证据可见、样本可回放、校准可解释”的方向做:

1. **把基本面证据展示到前端/API**
   - 展示质量评分、估值分位、数据完整度、价值陷阱四个核心指标。
   - 展示基本面矩阵位置，例如“好公司+高估”“弱公司+低估”。
   - 把 confidence 被压低的原因直接展示出来，避免用户只看到一个低置信度数字。

2. **让 Aggregator 直接消费结构化基本面摘要**
   - 现在 `data_summary` 已经包含 `decision_matrix`、`evidence`、`confidence_constraints` 和 `consistency_issues`。
   - 下一步应让汇总层读取这些字段，而不是只读自然语言 reasoning。
   - 当公司前景分析师被标记 degraded 时，汇总层应降低它对最终方向的投票权重。

3. **建立固定离线样本集**
   - 把好公司低估、差公司低估、好公司高估、数据严重缺失四类场景整理成 JSON fixture。
   - 每次改 Prompt、评分卡、估值分位或校准器时，都跑同一批样本。
   - 用 fixture 固定输出方向、风险提示、confidence 上限和矩阵位置，减少“看起来优化了但行为漂移”的问题。

4. **补港股和美股估值分位**
   - 当前 A 股更容易拿到历史 PE 分位，港股和美股容易退化成只看当前 PE。
   - 建议给不同市场接入可回放的历史估值序列，至少保证 PE/PB/PS 的 1 年或 3 年分位可用。
   - 如果分位缺失，应明确显示“估值分位未知”，并继续限制置信度。

5. **补盈利质量证据**
   - 增加经营现金流、扣非净利润、应收账款、存货、毛利率稳定性等字段。
   - 重点识别“利润增长但现金流恶化”“营收增长但应收账款异常上升”“低 PE 但盈利质量下滑”的场景。
   - 这些字段应进入价值陷阱判断，而不是只作为 Prompt 文本。

6. **做基本面历史快照回放**
   - 当前验证闭环已经能把预测结果写回校准器，但还需要保证回测时不使用未来财务数据。
   - 建议保存每次预测时实际看到的基本面快照，后续验证和回放都基于快照，而不是重新拉最新财务数据。

7. **做校准报告**
   - 输出公司前景分析师按数据质量、评分卡评级、估值分位区间分组后的命中率。
   - 当某个分组样本太少时，校准器应回退到保守先验，不要过度相信小样本。
   - 建议在报告里展示“样本数、平均置信度、实际命中率、校准偏差”。

8. **加入基本面催化剂日历**
   - 短期预测中，基本面通常需要财报、业绩预告、分红、回购、评级调整、订单公告等催化剂才能提高权重。
   - 建议把催化剂显式接入公司前景分析师，并作为短期置信度能否突破 0.60 的必要条件之一。

## 12. 第三轮执行记录：证据可见 + 汇总层消费 + 固定样本

本轮继续执行了上面再下一轮建议中的前三项:

| 文件 | 修改 |
| --- | --- |
| `src/agents/aggregator.py` | 新增公司前景结构化证据提取，读取 `decision_matrix`、`confidence_constraints`、估值分位、价值陷阱和一致性问题 |
| `src/agents/aggregator.py` | Aggregator Prompt 新增“结构化基本面证据”表，要求公司前景自然语言结论服从矩阵、价值陷阱和置信上限 |
| `src/agents/aggregator.py` | Agent 质量评分开始使用基本面结构化证据，矩阵建议方向与输出方向冲突时降低权重因子 |
| `src/core/result.py` | `FinalReport.to_dict()` 新增 `fundamental_evidence`，API 可直接读取公司前景证据摘要 |
| `src/core/result.py` | `FinalReport.to_markdown()` 新增“公司前景证据摘要”，展示矩阵、建议方向、质量评分、PE 分位、价值陷阱和置信上限 |
| `tests/fixtures/fundamental_scenarios.json` | 新增固定离线样本集，覆盖好公司低估、弱公司低估且价值陷阱、好公司高估、数据严重缺失 |
| `tests/test_phase1.py` | 新增 Aggregator 消费公司前景结构化证据的测试 |
| `tests/test_fundamental_v3.py` | 新增 fixture 回放测试，保证四类样本稳定映射到预期基本面矩阵 |

这轮优化后，公司前景分析师的结构化证据已经不只停留在自身结果里，而是进入了三个下游位置:

1. **Aggregator Prompt**: 汇总分析师会看到基本面矩阵和置信上限。
2. **权重调整**: 公司前景被 degraded、矩阵冲突或置信上限偏低时，会进一步降低权重。
3. **报告/API 输出**: `fundamental_evidence` 可以直接给前端或外部调用方展示。

已运行:

```bash
pytest -q tests/test_phase1.py tests/test_fundamental_v3.py tests/test_phase3.py
```

结果:

```text
75 passed, 2 deselected
```

第三轮之后，剩余更适合下一次做的优化是:

1. 补港股/美股历史估值分位。
2. 补经营现金流、扣非净利润、应收账款、存货等盈利质量证据。
3. 建立基本面历史快照回放，避免回测读取未来财务数据。
4. 做公司前景校准报告，展示分组样本数、平均置信度、实际命中率和校准偏差。
5. 加入基本面催化剂日历，提升短期基本面分析的解释力。

## 13. 五个 Agent 下一优先级判断

本轮优化结束后，五个 Agent 的优先级建议如下:

| 优先级 | Agent | 判断 |
| --- | --- | --- |
| 1 | 行业对比分析师 | 最需要立即优化 |
| 2 | 国际形势分析师 | 需要补测试和结构化回写，但已有两步推理和基础校验 |
| 3 | 公司前景分析师 | 本轮已经补上证据矩阵、验证回写、Aggregator 消费和固定 fixture，下一步进入数据源增强 |
| 4 | 最新新闻分析师 | 已有一致性校验测试，后续重点是事件溯源和影响衰减 |
| 5 | 近期股价分析师 | 已有较完整的技术证据包、校准、快照约束和专项测试 |

当务之急最应该优化的是 **行业对比分析师**。

原因:

1. 行业对比分析师直接影响“公司相对行业是否便宜/贵、行业景气度是否支撑估值”的判断，会和公司前景分析师共同放大估值类结论。
2. 当前 `_validate_consistency()` 只记录 warning，不会像公司前景分析师一样把问题写入 `risks/reasoning/status`，Aggregator 很难知道它的结论是否需要降权。
3. 当前结果没有稳定的结构化 `data_summary`，行业排名、性价比、行业趋势、轮动、产业链、催化剂没有进入统一证据包。
4. 目前没有看到行业对比分析师的专项测试覆盖；而技术面、新闻面、公司前景都已经有更明确的护栏测试。
5. 它与公司前景分析师职责相邻，下一轮优化行业对比分析师，可以让“公司自身质量 + 行业相对位置”形成更完整的交叉验证。

建议下一轮行业对比分析师优化主题:

1. 行业证据包: 行业排名、PE/ROE 分位、行业趋势、轮动信号、产业链影响、催化剂、数据质量。
2. 一致性护栏: 高估仍看涨、低估仍看跌、行业下行仍高置信看涨、数据缺失仍高置信等场景要写回结果。
3. Aggregator 消费: 让汇总层能看到行业证据包，并在行业结论 degraded 时降低权重。
4. 固定 fixture: 覆盖“行业强势+标的低估”“行业弱势+标的高估”“行业轮动转弱”“数据严重缺失”四类样本。
5. 验证闭环: 按行业、数据质量、估值分位追踪行业对比分析师历史命中率。
