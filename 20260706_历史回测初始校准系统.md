# 20260706 历史回测初始校准系统

## 1. 设计结论

你的设想可以拆成两层:

1. 历史数据先让 Agent 做预测，再用未来真实走势验证，生成初始校准样本。
2. 如果某类场景长期预测错，不直接自动改 MCP、skill、prompt，而是生成可审计的工程改进建议。

这两层相关，但不是完全同一件事。

- 校准层解决的是: 这个 Agent 在某个证据场景下 confidence 应该打几折。
- Agent 工程改进层解决的是: 失败主要来自 prompt 解释方式、skill 特征工程、MCP/数据源，还是 Aggregator 权重/裁决。

本轮实现采用保守闭环:

```text
历史截面/新闻快照 -> 代码证据矩阵预测 -> 未来价格验证
-> 写入专用校准器 -> 生成 Agent 工程改进建议
-> 人工审阅后再改 prompt/skill/MCP
```

不让系统根据少量样本自动改 prompt 或 MCP 配置，是为了避免被噪声样本带偏。

## 2. 本轮已落地

### 2.1 技术面专用校准器

新增 `TechnicalConfidenceCalibrator`。

它按以下场景桶统计历史命中率:

- 置信度区间
- 趋势桶
- 动量桶
- 量能桶
- 支撑/压力位置桶
- 盘中状态桶
- 预测周期桶

用途:

- 历史回测可以主动写入初始样本。
- 真实预测到期验证后也会继续写入样本。
- 近期股价分析师运行时会读取这些历史桶，对 confidence 做二次校准。

### 2.2 新闻面专用校准器

新增 `NewsConfidenceCalibrator`。

它按以下场景桶统计历史命中率:

- 置信度区间
- 新闻数量桶
- 来源数量桶
- 情绪桶
- 事件桶
- 时效桶

用途:

- 新闻面如果有历史新闻快照，可以生成初始样本。
- 真实预测到期验证后也会继续写入样本。
- 最新新闻分析师运行时会读取这些历史桶，对 confidence 做二次校准。

### 2.3 历史校准启动器

新增 `src/core/calibration_bootstrap.py`。

包含两条路径:

| 启动器 | 当前能力 |
| --- | --- |
| `TechnicalCalibrationBootstrapper` | 使用 `PriceFetcher.fetch_as_of()` 获取历史 K 线截面，避免未来数据泄漏 |
| `NewsSnapshotCalibrationBootstrapper` | 使用本地归档新闻快照生成新闻面样本 |

技术面现在可以直接跑历史区间。

新闻面没有假装能凭空恢复历史新闻；它要求使用本地新闻快照或后续接入可靠历史新闻源。

### 2.4 CLI

新增:

```bash
python scripts/bootstrap_calibration.py \
  --targets 000001,002396 \
  --start 2025-01-01 \
  --end 2025-12-31 \
  --timeframe "短期(1周)" \
  --interval 7
```

如果已有新闻快照:

```bash
python scripts/bootstrap_calibration.py \
  --targets 000001 \
  --start 2025-01-01 \
  --end 2025-12-31 \
  --news-snapshots data/news_snapshots
```

输出报告会包含:

- 样本数
- 方向命中率
- 跳过样本原因
- 校准统计
- Agent 工程改进建议

### 2.5 在线验证闭环

`PredictionStore` 已经扩展:

- `近期股价分析师` 验证后写入技术面校准器。
- `最新新闻分析师` 验证后写入新闻面校准器。

因此现在闭环变成:

```text
历史回测初始样本 + 真实运行验证样本 -> 同一套专用校准器
```

### 2.6 Agent 工程改进建议器

新增 `AgentImprovementAdvisor`。

它会把低命中率场景翻译成工程建议:

| 低命中场景 | 优先建议 |
| --- | --- |
| 技术面支撑/压力或量能桶失败 | prompt 规则和风险降级 |
| 技术面趋势/动量/盘中桶失败 | skill 特征工程和阈值校验 |
| 新闻来源/时效桶失败 | MCP/新闻源和快照归档 |
| 新闻情绪/事件桶失败 | prompt 事件归因和传闻识别 |
| 置信度区间失败 | calibration 上限调整 |

它只生成建议，不自动改文件。

## 3. 后续仍需完善的部分

### 3.1 新闻历史快照库

本轮已经完成第一版新闻快照归档与回放:

- 最新新闻分析师运行完成后会自动归档新闻快照。
- 快照保存原始新闻、预处理摘要、证据包、最终预测和预测周期。
- `scripts/replay_news_snapshots.py` 可以批量回放快照并写入新闻面校准器。
- `scripts/bootstrap_calibration.py` 已复用统一快照读取接口。

仍需注意: 快照只能复盘系统运行当时看到的新闻，不能补回过去没有运行时的完整历史新闻。

### 3.2 自动 prompt/skill/MCP 变更仍需人工审阅

当前系统不会自动改 prompt、skill 或 MCP 配置。

推荐后续做成:

```text
改进建议 -> 生成补丁草案 -> 人工确认 -> 执行修改 -> 再回测验证
```

这样既能进化 Agent，又不会让小样本噪声直接控制系统设计。

## 4. 推荐下一轮

下一轮最值得做:

```text
基于新闻快照回放结果生成 prompt/MCP 改进补丁草案
```

原因:

1. 技术面历史校准已经能启动。
2. 新闻快照归档与回放已经打通。
3. 下一步应该让回放统计真正推动 Agent 工程改进: 哪些问题该改 prompt，哪些该补 MCP 新闻源，哪些该改 skill 特征工程。
