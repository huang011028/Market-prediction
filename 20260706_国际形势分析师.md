# 20260706 国际形势分析师优化建议

## 1. 当前状态

国际形势分析师已经具备 v2 的基础能力:

- 使用 `MacroFetcherV2` 拉取中国宏观、汇率、美国利率、DXY、VIX 等数据。
- 注入 `stock_context`，识别标的行业和宏观敏感因子。
- 使用两步推理: 宏观环境评估 → 标的传导。
- 可选接入地缘政治事件。
- 有数据新鲜度提示和基础一致性校验。

但当前问题是: 宏观证据没有变成稳定的结构化约束，校验问题也没有充分回写到结果和验证闭环。

## 2. 主要问题

| 问题 | 影响 |
| --- | --- |
| 宏观证据没有统一证据包 | Aggregator/API/历史记录难以复用 |
| 标的传导依赖 LLM 自由解释 | 容易忽略行业敏感度，例如利率对银行和互联网方向相反 |
| 一致性校验只追加 reasoning | `risks/status/confidence` 不一定同步反映降级 |
| 低新鲜度和参考值占比高时约束不够统一 | 可能出现低质量数据下的高置信方向判断 |
| 没有宏观验证回写校准器 | 预测验证后无法按市场/行业/数据质量修正置信度 |

## 3. 本轮优化目标

本轮建议优先完成:

1. **宏观证据包**
   - 汇总中国宏观、美国利率、DXY、VIX、汇率、地缘风险、数据质量和标的宏观敏感度。

2. **宏观传导矩阵**
   - 用代码计算宏观环境对标的的 suggested_direction，区分银行、互联网、消费、能源、医药等敏感度差异。

3. **一致性护栏回写**
   - 高地缘风险却高置信看涨、低新鲜度高置信、利率敏感行业不提利率、参考值过多仍高置信等，要写入 `risks/reasoning/status`。

4. **结构化摘要**
   - 输出 `data_summary`，让 API、报告、Aggregator 和历史记录可以读取宏观证据。

5. **验证闭环**
   - 预测验证后，把国际形势分析师样本回写宏观校准器。

## 4. 建议证据包结构

```yaml
identity:
  symbol / name / market / sector / data_source
macro_snapshot:
  china:
    pmi / cpi / gdp / lpr / m2
  us:
    fed_funds / us_10y / vix
  forex:
    usd_cny / usd_cnh / dxy
geopolitical:
  risk_level / risk_score / trend / key_themes / recent_events
stock_context:
  inferred_sector / macro_sensitivity / transmission_hints / market_note
data_quality:
  realtime_count / reference_count / overall_freshness
decision_matrix:
  macro_regime / sector / suggested_direction / reason
evidence:
  bullish / bearish / neutral
confidence_constraints:
  ceiling / max_confidence / hard_caps
```

## 5. 本轮执行计划

| 步骤 | 文件 |
| --- | --- |
| 生成宏观证据包和传导矩阵 | `src/agents/macro_analyst.py` |
| 把证据包注入两步推理和单 pass 回退 | `src/agents/macro_analyst.py` |
| 校验问题写回结果并降权 | `src/agents/macro_analyst.py` |
| 输出结构化 `data_summary` | `src/agents/macro_analyst.py` |
| 新增宏观校准器 | `src/utils/macro_calibrator.py` |
| 预测验证后回写宏观校准器 | `src/data/prediction_store.py` |
| 固定四类离线样本 | `tests/fixtures/macro_scenarios.json` |
| 补 MacroAnalyst 护栏测试 | `tests/test_macro_analyst_v2.py` |

## 6. 下一轮建议

本轮之后还值得继续做:

1. 让 Aggregator 直接消费宏观证据包。
2. 把最终报告展示为“宏观顺风/逆风 + 标的敏感因子 + 置信上限”。
3. 引入宏观历史快照，避免回测时使用未来宏观数据。
4. 按市场、行业、宏观 regime 生成校准报告。
5. 增加更细的事件日历，例如 FOMC、CPI、PMI、非农、中央经济工作会议等。

## 7. 本轮已执行优化

本轮已经完成以下修改:

| 文件 | 修改 |
| --- | --- |
| `src/agents/macro_analyst.py` | 新增宏观证据包，包含中国宏观、美国利率、DXY、VIX、汇率、地缘风险、标的敏感度和数据质量 |
| `src/agents/macro_analyst.py` | 新增宏观环境-标的传导矩阵，区分银行、互联网、消费、能源等行业对利率/汇率/地缘风险的不同方向 |
| `src/agents/macro_analyst.py` | Step 2 和单 pass 回退都注入代码计算的宏观证据包 |
| `src/agents/macro_analyst.py` | 新增低新鲜度、参考值过多、高 VIX、高 DXY、高地缘风险等一致性护栏 |
| `src/agents/macro_analyst.py` | 护栏问题写回 `risks/reasoning/status`，并降低 confidence |
| `src/agents/macro_analyst.py` | 新增结构化 `data_summary`，供 API、历史记录和后续 Aggregator 使用 |
| `src/utils/macro_calibrator.py` | 新增 `MacroConfidenceCalibrator`，按置信度、市场、行业、数据质量追踪历史命中率 |
| `src/data/prediction_store.py` | 预测验证完成后，国际形势分析师样本会回写宏观校准器 |
| `tests/fixtures/macro_scenarios.json` | 新增四类固定离线样本 |
| `tests/test_macro_analyst_v2.py` | 新增 MacroAnalyst 护栏测试和宏观校准器测试 |
| `tests/test_phase3.py` | 新增国际形势分析师验证回写校准器测试 |

## 8. 验证结果

已运行:

```bash
pytest -q tests/test_macro_analyst_v2.py tests/test_phase3.py
```

结果:

```text
42 passed, 5 deselected
```

并已运行:

```bash
python3 -m py_compile src/agents/macro_analyst.py src/data/prediction_store.py src/utils/macro_calibrator.py
```

结果: 通过。

## 9. 本轮之后的下一步

国际形势分析师已经从“宏观数据 + LLM 传导”推进到“宏观证据包 + 标的敏感度矩阵 + 护栏回写 + 验证闭环”。下一轮最值得做的是让 Aggregator 直接消费宏观证据包，并在最终报告里展示宏观顺风/逆风、主要传导因子和置信上限。
