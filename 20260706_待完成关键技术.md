# 20260706 待完成关键技术

## 1. 当前项目状态

目前项目已经完成了一个重要阶段:

- 5 个 Agent 基本都进入了结构化证据体系。
- 公司前景、行业对比、国际形势、最新新闻、近期股价都已经能输出可被 Aggregator 消费的证据包。
- Aggregator 已经具备结构化证据消费、动态降权和最终裁决复核能力。
- 基本面、行业、宏观已经接入验证后的专用校准器。

现在项目的主要瓶颈，已经从“单个 Agent 会不会乱说”转向“系统能不能用历史样本证明自己哪些场景可信”。

## 2. P0: 历史回测生成初始校准样本

### 目标

用历史回测给技术面和新闻面生成初始校准样本，而不是等手动运行很多次以后慢慢积累。

### 为什么重要

当前校准器主要依赖在线验证:

```text
实时预测 -> 到期验证 -> 写入校准统计 -> 下一次预测时修正置信度
```

这种方式可靠，但冷启动很慢。尤其是技术面和新闻面，如果没有足够已验证样本，校准器无法知道:

- 技术面在“放量突破”场景下真实命中率是多少。
- 技术面在“接近压力位但未放量”场景下是否经常过度乐观。
- 新闻面在“新闻稀少但正面”场景下是否容易虚高置信度。
- 新闻面在“情绪分化/传闻主导”场景下真实方向命中率是多少。

历史回测可以主动生成这些初始样本，让系统一开始就带着经验进入实时预测。

### 2026-07-06 执行状态

本轮已完成第一版历史回测初始校准系统:

- 已新增 `TechnicalConfidenceCalibrator`，技术面可按趋势、动量、量能、支撑压力、盘中状态和周期进行历史桶校准。
- 已新增 `NewsConfidenceCalibrator`，新闻面可按新闻数量、来源、情绪、事件类型和时效进行历史桶校准。
- 已新增 `TechnicalCalibrationBootstrapper`，可用历史 K 线截面主动生成技术面初始校准样本。
- 已新增 `NewsSnapshotCalibrationBootstrapper`，可用已归档新闻快照生成新闻面初始校准样本。
- 已新增 `AgentImprovementAdvisor`，把低命中率场景翻译成 prompt、skill、MCP 或 calibration 改进建议。
- `PredictionStore` 已接入技术面和新闻面的验证后校准回写。
- 近期股价分析师和最新新闻分析师运行时会读取专用历史桶，对 confidence 做二次校准。

剩余关键点是新闻历史快照库: 新闻面已有校准器和回放接口，但还需要稳定归档真实历史新闻，才能像技术面 K 线一样批量冷启动。

### 建议实现

1. 选择股票池和历史区间。
2. 对每个历史日期构造当时可见的数据快照，禁止使用未来数据。
3. 运行技术面和新闻面证据矩阵，记录:
   - 预测方向
   - 置信度
   - 证据桶
   - 数据质量
   - 技术/新闻场景标签
4. 用未来真实涨跌验证。
5. 写入技术面和新闻面专用校准器的统计文件。

### 技术面校准桶建议

```yaml
confidence_bins:
  0.0-0.2 / 0.2-0.4 / 0.4-0.6 / 0.6-0.8 / 0.8-1.0
trend_buckets:
  up / down / sideways / unknown
momentum_buckets:
  bullish / bearish / mixed
volume_buckets:
  confirm_up / confirm_down / shrinking / neutral
position_buckets:
  near_resistance / near_support / middle_range
intraday_buckets:
  strong_up / selloff / mixed / unavailable
timeframe_buckets:
  短期 / 中期 / 长期
```

### 新闻面校准桶建议

```yaml
confidence_bins:
  0.0-0.2 / 0.2-0.4 / 0.4-0.6 / 0.6-0.8 / 0.8-1.0
news_count_buckets:
  none / sparse / limited / adequate / rich
source_buckets:
  single_source / multi_source
sentiment_buckets:
  positive / negative / neutral / divergent / unknown
event_buckets:
  positive_catalyst / negative_catalyst / mixed / rumor_driven / no_clear_event
freshness_buckets:
  fresh / decayed / stale
```

## 3. P1: 回测快照与防未来数据泄漏

历史校准最容易出错的地方是未来数据泄漏。必须确保历史日期的预测只能看到当时之前的数据。

关键要求:

- 技术面只使用 `as_of` 当天及之前 K 线。
- 新闻面只使用 `as_of` 当天及之前新闻。
- 基本面、行业、宏观如果进入回测，也要使用当时能看到的数据版本。
- 回测结果需要记录快照日期、验证日期和预测周期。

这部分完成后，回测校准才可信。

## 4. P1: 技术面与新闻面专用校准器

当前已有基本面、行业、宏观校准器。本轮已新增:

| 校准器 | 作用 |
| --- | --- |
| `TechnicalConfidenceCalibrator` | 按趋势、动量、量能、支撑压力、盘中状态校准技术面 confidence，已接入历史启动和在线验证回写 |
| `NewsConfidenceCalibrator` | 按新闻数量、来源、情绪、事件类型、时效校准新闻面 confidence，已接入新闻快照启动接口和在线验证回写 |

它们已经支持:

- `calibrate()`
- `update_from_validation()`
- `get_calibration_stats()`
- `save()`

并接入 `PredictionStore` 的验证回写流程。

## 5. P1: 校准报告与可解释审计

校准器不应该只是静默修改 confidence，还应该输出可检查的报告。

建议新增:

- 每个 Agent 的置信度桶命中率。
- 每个场景桶的样本数和准确率。
- 哪些场景长期过度自信。
- 哪些场景长期低估自己。
- Aggregator 最终裁决前后的方向和置信度变化。

这会让后续优化不再靠感觉，而是看统计结果。

### 2026-07-06 追加执行状态

本轮已新增第一版项目级历史评估闭环:

- 新增 `src/core/historical_evaluator.py`，统一消费历史校准样本、新闻快照回放报告和已验证 PredictionStore 记录。
- 新增 `scripts/evaluate_historical_samples.py`，可生成历史评估 JSON/Markdown，并按 Agent 输出改进草案。
- 评估报告现在包含:
  - 每个 Agent 的历史命中率、平均置信度和校准缺口。
  - 每个证据桶的命中率和过度自信程度。
  - Aggregator 历史贡献归因: 哪些 Agent 在帮助最终正确，哪些 Agent 在强化最终错误。
  - 错误策略信号: 低命中或过度自信的 prompt / MCP / skill / data_source / calibration 场景。
  - 可保留优势信号: 历史命中率稳定较高的证据场景。

这一步把“哪些策略有效”和“哪些策略错误”都纳入同一套可审计输出，不再只靠单次前端预测来判断。

### 2026-07-06 再追加执行状态

本轮已把历史评估从“只读取已验证预测库”推进到“主动历史样本池”:

- 新增 `src/core/self_improvement_lab.py`，可主动选择股票池和历史区间生成真实历史样本。
- 新增 `scripts/run_self_improvement_lab.py`，命令行可直接运行主动历史样本实验室。
- 前端“调优”页新增“主动历史样本池”，可以从页面触发历史样本构造、历史评估和 Agent 改进工程师。
- Agent 改进工程师新增 `min_unique_cases_for_auto_apply`，自动写 prompt/skill 必须同时满足总样本数和独立历史案例数。
- Agent 改进工程师现在只对 `wrong_strategy` 信号生成自动动作，`strength` 信号只作为保留优势参考。

这意味着调优链路的主入口不再只是 `data/predictions.db`。`predictions.db` 更适合作为线上预测到期后的补充验证，主动样本池才是批量研究“哪些策略真的有预测能力”的主路径。

### 2026-07-06 第三次追加执行状态

本轮已补齐公司前景、行业对比、国际形势的 point-in-time 快照源第一版:

- 新增 `src/data/point_in_time_snapshot_archive.py`
  - 统一保存/读取基本面、行业、宏观的历史时点快照。
  - 支持目录、JSON、JSONL。
  - 标准字段为 `agent_name`、`target`、`symbol`、`market`、`as_of`、`valid_date`、`data`、`stock_context`。
- 新增三类快照回放器:
  - `FundamentalSnapshotCalibrationBootstrapper`
  - `IndustrySnapshotCalibrationBootstrapper`
  - `MacroSnapshotCalibrationBootstrapper`
- 主动历史样本池现在可同时消费:
  - 历史 K 线: 近期股价分析师
  - 新闻快照: 最新新闻分析师
  - point-in-time 快照: 公司前景、行业对比、国际形势
- 新增 `scripts/archive_point_in_time_snapshots.py`
  - 从当前日期开始持续归档基本面/行业/宏观可见数据。
  - 这些快照到期后可用于真实回放验证。
- 前端“主动历史样本池”新增 point-in-time 快照路径输入。

注意: 这一步补齐的是“快照源与回放入口”。对于过去没有归档过的基本面/行业/宏观数据，系统仍不会用当前最新数据倒填历史，避免未来数据泄漏。

## 6. P2: 新闻历史数据归档

技术面历史 K 线比较容易回放，新闻面更难，因为新闻源通常拿不到完整历史快照。

2026-07-06 本轮已完成第一版新闻快照归档与回放:

1. `NewsAnalyst` 每次完成分析后会保存新闻快照。
2. 默认快照库为 `data/news_snapshots/`。
3. 快照包含原始新闻、预处理摘要、证据包、最终预测、预测周期和校准所需字段。
4. `scripts/replay_news_snapshots.py` 可以批量回放快照。
5. `scripts/bootstrap_calibration.py` 已复用统一新闻快照读取接口。

仍需注意: 这解决的是“从现在开始归档并复盘”，不能补回过去没有运行时的完整历史新闻。

## 7. P2: Agent 贡献度回测

Aggregator 现在能消费结构化证据，但还需要长期回答一个问题:

```text
最终预测到底是哪个 Agent 帮了忙，哪个 Agent 在拖后腿？
```

建议在回测里记录:

- 每个 Agent 的单独方向是否正确。
- Aggregator 最终方向是否正确。
- 如果去掉某个 Agent，最终结果是否更好。
- 哪些 Agent 在不同周期下贡献最大。

这可以反过来优化权重配置。

### 2026-07-06 追加执行状态

第一版贡献归因已经落地到 `HistoricalAgentEvaluator`:

- 如果某个 Agent 与最终裁决同向且最终正确，记为帮助最终正确。
- 如果某个 Agent 与最终裁决同向但最终错误，记为强化最终错误。
- 如果某个 Agent 与最终裁决相反但事后正确，记为被忽略但正确的反向证据。
- 如果某个 Agent 与最终裁决相反且自身错误，而最终正确，记为被正确折价。

当“强化最终错误”或“被忽略但正确”的比例达到阈值时，系统会生成面向 Aggregator 的改进信号，用来调整最终裁决权重、异议证据可见性和校准硬上限。

## 8. 推荐下一步

历史回测初始校准系统、新闻快照归档与回放、项目级历史评估闭环第一版已经落地。现在我最推荐下一步优先做:

```text
积累足够 point-in-time 快照样本，并开始按 5 个 Agent 的历史有效性调整 prompt / skill / 数据源策略
```

当前可以先用 `scripts/run_self_improvement_lab.py` 形成固定流程:

```text
主动历史样本池 -> 历史评估报告 -> 错误策略信号 -> 改进工程师 -> prompt/skill 自动改进 + 高风险建议 -> 再回放验证
```

原因:

1. 技术面历史校准已经可以用 K 线直接启动。
2. 新闻面现在已经能自动积累快照，并能在提供快照文件后批量回放。
3. 历史评估器已经能同时识别“有效策略”和“错误策略”。
4. 自动改 prompt/skill 已加入独立案例数门槛，降低小样本过拟合风险。
5. 基本面、行业、宏观已经有快照源与回放器，下一步的最大缺口是样本积累和快照质量。

推荐执行顺序:

1. 先用 A 股、港股、美股混合股票池跑主动历史样本池，验证技术面样本质量。
2. 回放已有新闻快照；同时从今天开始持续归档更多新闻快照。
3. 每天或每周运行 `scripts/archive_point_in_time_snapshots.py`，积累公司前景/行业/宏观快照。
4. 到期后运行 `scripts/run_self_improvement_lab.py --point-in-time-snapshots data/point_in_time_snapshots`。
5. 检查 5 个 Agent 的低命中桶、独立样本数和场景分布。
6. 让 Agent 改进工程师批量提出 prompt / MCP / skill / 数据源策略建议，并用留出快照复测。
