# 20260706 数据源覆盖率巡检系统

## 背景

前端真实企业测试暴露出一个核心问题：单个企业能跑通，不代表每个 agent 的数据源覆盖稳定。不同市场、行业、公司规模会触发不同数据源路径，例如 A 股、港股、美股、中文名解析、新闻源过滤、港股财务、行业 peer、宏观实时指标等。

因此新增一个后端巡检系统，专门批量检查每个 agent 的数据采集层，不跑 LLM 推理，避免把模型输出问题和数据源缺失混在一起。

## 已完成

新增核心模块：

- `src/core/data_source_auditor.py`

新增命令行脚本：

- `scripts/audit_data_sources.py`

新增测试：

- `tests/test_data_source_auditor.py`

## 巡检范围

系统会直接调用每个 agent 的 `gather_data()`：

- `近期股价分析师`: 行情、技术指标、日线/分钟趋势、技术数据质量
- `最新新闻分析师`: 新闻数量、新闻源、预处理质量、可用性
- `公司前景分析师`: 财务、估值、数据完整度、缺失字段
- `国际形势分析师`: 中国宏观、美国宏观、汇率、实时/参考值比例
- `行业对比分析师`: 行业分类、标的指标、行业均值、peer、行业质量

巡检不会调用 LLM，因此不会消耗模型 token。

## 使用方式

默认跑一篮子 A/HK/US 标的：

```bash
python scripts/audit_data_sources.py
```

指定标的：

```bash
python scripts/audit_data_sources.py --targets "京东,小米集团,网易,中芯国际,AAPL,NVDA"
```

只检查某几个 agent：

```bash
python scripts/audit_data_sources.py --agents "行业对比分析师,公司前景分析师"
```

提高并发：

```bash
python scripts/audit_data_sources.py --concurrency 2 --timeout 90
```

输出位置默认在：

```text
output/data_source_audits/
```

每次会生成：

- `data_source_audit_YYYYMMDD_HHMMSS.json`
- `data_source_audit_YYYYMMDD_HHMMSS.md`

## 状态定义

- `ok`: 数据质量较高且关键字段完整
- `partial`: 可分析，但存在非关键缺口或质量偏低
- `poor`: 关键字段缺失、质量很低或主要数据源不可用
- `failed`: 数据采集抛异常或超时

## 读取报告的重点

优先看三个部分：

1. 总览中的 `poor` / `failed` 数量
2. 标的明细里每个 agent 的缺失字段
3. 高频问题里跨标的重复出现的缺口

如果某个字段在多个标的上重复缺失，优先级高于单个企业的偶发缺失。

## 当前建议

下一步可以把这个巡检报告接入前端或管理页，让前端测试前先跑一次“数据源体检”。这样可以在用户看到“某个 agent 数据完全缺失”之前，就提前知道是哪条数据源链路坏了。
