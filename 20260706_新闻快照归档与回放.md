# 20260706 新闻快照归档与回放

## 1. 目标

把最新新闻分析师从“一次性实时分析”升级为:

```text
实时采集 -> 结构化分析 -> 自动归档新闻快照 -> 后续批量回放 -> 写入新闻面校准器
```

这样新闻面后续可以复盘、回测和校准，不再只能依赖当次实时新闻。

## 2. 本轮已落地

### 2.1 新闻快照归档模块

新增 `src/data/news_snapshot_archive.py`。

核心能力:

- `save_analysis_snapshot()` 保存一次新闻分析快照。
- `load_snapshots()` 读取目录、单个 JSON 或 JSONL。
- `attach_prediction_id()` 在 `PredictionStore` 保存后，把预测 ID 追加回快照。
- `export_jsonl()` 将快照集合导出为 JSONL。
- 自动维护 `data/news_snapshots/index.jsonl` 最小索引。

默认快照目录:

```text
data/news_snapshots/
```

单个快照路径类似:

```text
data/news_snapshots/000001/20260706/000001_20260706_101530_短期_1周__a1b2c3d4.json
```

### 2.2 新闻分析师自动归档

`NewsAnalyst._finalize_result()` 已接入快照归档。

每次最新新闻分析师完成分析后，会保存:

- 标的、市场、预测周期
- as_of 日期和 valid_date
- 原始新闻列表
- 预处理摘要
- 数据质量
- 新闻证据包
- step signals
- 最终方向、置信度、reasoning
- 新闻数量和来源

如果 `PredictionStore.save_prediction()` 成功，`scripts/run_analysis.py` 会把 prediction_id 回写到快照文件。

### 2.3 新闻快照回放

新增专用 CLI:

```bash
python scripts/replay_news_snapshots.py --snapshots data/news_snapshots
```

按标的和日期过滤:

```bash
python scripts/replay_news_snapshots.py \
  --snapshots data/news_snapshots \
  --target 000001 \
  --start 2026-01-01 \
  --end 2026-06-30
```

输出:

- 成功样本数
- 跳过样本原因
- 方向命中率
- 新闻面校准统计
- Agent 工程改进建议

### 2.4 历史校准 CLI 复用新闻快照

`scripts/bootstrap_calibration.py` 已改为通过 `NewsSnapshotArchive` 读取新闻快照。

示例:

```bash
python scripts/bootstrap_calibration.py \
  --targets 000001,002396 \
  --start 2026-01-01 \
  --end 2026-06-30 \
  --news-snapshots data/news_snapshots
```

## 3. 快照格式

快照顶层结构:

```json
{
  "schema_version": "news_snapshot.v1",
  "snapshot_id": "...",
  "archived_at": "...",
  "target": "000001",
  "symbol": "000001",
  "market": "A",
  "timeframe": "短期(1周)",
  "as_of": "2026-07-06",
  "valid_date": "2026-07-13",
  "prediction_id": "可选",
  "news_data": {},
  "step_signals": {},
  "analysis_result": {},
  "evidence": {},
  "predicted_direction": "bearish",
  "predicted_confidence": 0.58,
  "news_count": 4,
  "sources_used": ["eastmoney", "sina"]
}
```

回放时优先使用快照里的最终预测:

```text
predicted_direction / predicted_confidence
```

如果旧快照没有最终预测，则回退到新闻证据矩阵:

```text
decision_matrix.suggested_direction / confidence_constraints.max_confidence
```

## 4. 控制开关

默认开启新闻快照归档。

临时关闭:

```bash
NEWS_SNAPSHOT_ARCHIVE=false python scripts/run_analysis.py --target 000001
```

## 5. 还需要注意

新闻快照记录的是“运行当时系统看到的新闻”，不是第三方完整历史新闻数据库。

因此:

- 它能复盘你自己的 Agent 团队当时为什么这么判断。
- 它能在预测到期后生成真实校准样本。
- 它不能补回过去没有运行时的历史新闻。

## 6. 推荐下一步

下一步最建议做:

```text
基于新闻快照回放结果生成 prompt/MCP 改进补丁草案
```

具体路径:

1. 先积累一批新闻快照。
2. 到期后用 `replay_news_snapshots.py` 回放。
3. 看低命中率场景集中在哪些桶:
   - 来源单一
   - 新闻时效弱
   - 情绪分化
   - 传闻主导
   - 事件归因错误
4. 让 `AgentImprovementAdvisor` 生成建议。
5. 再把建议转成 prompt、MCP 数据源或 skill 特征工程的补丁草案。

## 7. 2026-07-06 追加完成: 补丁草案生成

本轮已新增:

```text
src/core/improvement_patch_planner.py
scripts/build_agent_improvement_drafts.py
```

现在可以从新闻快照回放报告直接生成 prompt/MCP/skill 草案:

```bash
python scripts/build_agent_improvement_drafts.py \
  --replay-report output/news_snapshot_replay_all.json \
  --output-dir output/news_improvement_drafts
```

也可以在回放时直接生成:

```bash
python scripts/replay_news_snapshots.py \
  --snapshots data/news_snapshots \
  --draft-dir output/news_improvement_drafts
```

输出内容:

- `improvement_patch_plan.md`
- `improvement_patch_plan.json`
- prompt 草案
- MCP/数据源草案
- skill/特征工程草案
- calibration 草案

这些仍然是草案，不会自动改源码。
