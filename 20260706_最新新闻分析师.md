# 20260706 最新新闻分析师优化建议

## 1. 当前状态

最新新闻分析师已经具备 v2 的基础能力:

- 使用 `NewsFetcher` 多源采集东方财富、新浪、yfinance 等新闻。
- 预处理管线已经包含去重、相关度过滤、情绪预标注、事件分类、时间衰减和异常检测。
- 分析流程支持两步推理: 信号提取 → 综合判断 + 反思。
- 已有基础一致性校验，例如方向与加权情绪不一致、新闻数量不足却高置信、情绪分化等。
- 可选使用 `PredictionStore` 做通用置信度校准。

但目前最大的问题是: 新闻证据没有形成稳定的结构化约束，Aggregator 也不能直接消费新闻证据包。

## 2. 主要问题

| 问题 | 影响 |
| --- | --- |
| 一致性校验只追加 reasoning | Aggregator 和前端难以知道新闻结论是否需要降权 |
| 缺少结构化新闻证据包 | API、报告、历史记录只能读自然语言 |
| Step 2 Prompt 没有硬证据约束 | LLM 可能忽略新闻数量、来源单一、传闻主导和情绪分化 |
| 新闻事件没有矩阵化 | 业绩、政策、评级、传闻等事件的影响强弱没有统一表达 |
| Aggregator 只消费基本面证据 | 新闻、行业、宏观虽然有证据包，但汇总层仍容易按 reasoning 直接采信 |

## 3. 本轮优化目标

本轮建议优先完成五件事:

1. **新闻证据包**
   - 输出标的、新闻窗口、来源质量、情绪统计、事件分类、异常标记、top news 快照。

2. **新闻情绪-事件矩阵**
   - 用代码生成 `suggested_direction`，区分新闻稀少、情绪分化、正面催化、负面冲击、传闻主导等场景。

3. **一致性护栏回写**
   - 方向与情绪冲突、新闻少却高置信、情绪分化仍强方向等问题，要写入 `risks/reasoning/status`。

4. **置信度硬上限**
   - 新闻数量不超过2条、来源单一、数据质量低、传闻主导、情绪分化等场景，要限制 `confidence`。

5. **Aggregator 统一消费结构化证据**
   - 把基本面、行业、宏观、新闻证据包纳入统一的 `structured_evidence`，用于 prompt、权重调整和最终报告。

## 4. 建议的新闻证据包结构

```yaml
identity:
  symbol / name / market / news_source
news_window:
  date_range / news_count / sources_used
source_quality:
  quality_score / source_count / is_available
sentiment_stats:
  positive / negative / neutral / unknown / weighted_positive_score / weighted_negative_score
category_breakdown:
  earnings / policy / corp_action / rating / product / industry / rumor / other
anomaly_flags:
  sentiment_divergence / sudden_volume_spike
decision_matrix:
  volume_bucket / sentiment_bucket / event_bucket / suggested_direction / reason
event_impact_matrix:
  dominant_event / positive_event_weight / negative_event_weight / sentiment_spread / average_time_weight
evidence:
  bullish / bearish / neutral
confidence_constraints:
  max_confidence / quality_score / hard_caps
```

## 5. 本轮执行计划

| 步骤 | 文件 |
| --- | --- |
| 生成新闻证据包和情绪-事件矩阵 | `src/agents/news_analyst.py` |
| 把证据包注入单 pass、信号提取和综合判断 prompt | `src/agents/news_analyst.py` |
| 校验问题写回结果并降权 | `src/agents/news_analyst.py` |
| 输出结构化 `data_summary` | `src/agents/news_analyst.py` |
| 固定四类离线样本 | `tests/fixtures/news_scenarios.json` |
| 补 NewsAnalyst 护栏测试 | `tests/test_news_analyst_v2.py` |
| Aggregator 统一消费结构化证据 | `src/agents/aggregator.py`, `src/core/result.py` |
| 补 Aggregator 通用证据测试 | `tests/test_phase1.py` |

## 6. 本轮已执行优化

本轮已经完成以下修改:

| 文件 | 修改 |
| --- | --- |
| `src/agents/news_analyst.py` | 新增新闻证据包，包含新闻窗口、来源质量、情绪统计、事件分类、异常标记和 top news 快照 |
| `src/agents/news_analyst.py` | 新增新闻情绪-事件矩阵，输出 `suggested_direction`、矩阵位置和矩阵原因 |
| `src/agents/news_analyst.py` | 新增事件冲击矩阵，区分正面催化、负面冲击、多空并存、传闻主导 |
| `src/agents/news_analyst.py` | 单 pass、信号提取和综合判断 prompt 均注入代码计算的新闻证据包 |
| `src/agents/news_analyst.py` | 一致性校验和新闻证据约束写回 `risks/reasoning/status`，并降低 confidence |
| `src/agents/news_analyst.py` | 新增结构化 `data_summary`，供 API、历史记录和 Aggregator 使用 |
| `src/agents/aggregator.py` | 新增通用 `structured_evidence` 提取和评分，覆盖基本面、行业、宏观、新闻 |
| `src/agents/aggregator.py` | 汇总 prompt 新增结构化 Agent 证据表，并把矩阵冲突、硬上限纳入权重调整 |
| `src/core/result.py` | 最终报告导出 `structured_evidence`，同时保留 `fundamental_evidence` 兼容字段 |
| `tests/fixtures/news_scenarios.json` | 新增四类固定离线样本 |
| `tests/test_news_analyst_v2.py` | 新增 NewsAnalyst 证据包、护栏回写和 fixture 测试 |
| `tests/test_phase1.py` | 新增 Aggregator 通用结构化证据测试 |

## 7. 验证结果

已运行:

```bash
python3 -m py_compile src/agents/news_analyst.py src/agents/aggregator.py src/core/result.py
```

结果: 通过。

已运行:

```bash
pytest -q tests/test_news_analyst_v2.py tests/test_phase1.py tests/test_result.py
```

结果:

```text
75 passed, 2 skipped, 7 deselected
```

并已运行全量测试:

```bash
pytest -q
```

结果:

```text
278 passed, 2 skipped, 12 deselected
```

已运行:

```bash
git diff --check
```

结果: 通过。

## 8. 下一轮建议

本轮之后，新闻分析师还值得继续做:

1. 多源分歧检测: 东方财富、新浪、yfinance 对同一事件的情绪相反时，显式标记 `source_divergence`。
2. 标题党/反转检测: 标题正面但正文负面、传闻后被辟谣等场景要降低权重。
3. 历史相似案例注入: 从 `PredictionStore` 查询同一标的、相似情绪分布的历史预测和验证结果。
4. 新闻源权重自适应: 按历史验证结果动态调整不同来源的可信度。
5. Aggregator 最终报告继续增强: 展示各 Agent 结构化证据冲突点，而不仅是单张摘要表。
