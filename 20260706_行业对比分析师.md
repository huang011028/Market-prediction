# 20260706 行业对比分析师优化建议

## 1. 当前状态

行业对比分析师已经具备 v3 的基础框架:

- 使用 `IndustryFetcher.fetch_enhanced()` 获取行业对比数据。
- 接入行业排名、性价比评分、行业趋势和数据质量。
- 引入行业轮动检测、产业链分析、催化剂日历。
- 使用 `IndustryConfidenceCalibrator` 做历史置信度校准。
- 有工具层测试覆盖轮动检测、产业链、催化剂和校准器。

但目前最大的问题是: 这些结构化证据没有稳定进入最终结果和汇总层。

## 2. 主要问题

| 问题 | 影响 |
| --- | --- |
| 一致性校验只打日志 | Aggregator 不知道行业结论是否需要降权 |
| 缺少结构化 `data_summary` | API、报告、历史记录很难复用行业证据 |
| Step B Prompt 没有行业证据包 | LLM 仍可能把行业排名、性价比、趋势讲反 |
| 校准后没有再次强制数据质量 ceiling | 历史校准可能把低质量样本置信度抬高 |
| 缺少 IndustryAnalyst 自身护栏测试 | 工具层有测试，但 agent 输出行为没有被固定 |

## 3. 本轮优化目标

本轮建议优先完成五件事:

1. **行业证据包**
   - 输出行业排名、PE/ROE 分位、性价比评分、行业趋势、轮动信号、产业链、催化剂、数据质量。

2. **行业质量-估值-趋势矩阵**
   - 用代码先形成 suggested_direction，LLM 只能在这个证据框架内调整。

3. **一致性护栏回写**
   - 高估仍看涨、低估仍看跌、行业下行仍高置信看涨、数据缺失仍高置信等场景，要写入 `risks/reasoning/status`。

4. **结构化摘要**
   - `AnalysisResult.data_summary` 要能被 Aggregator、API、报告和历史验证读取。

5. **固定离线样本**
   - 覆盖行业强势+标的低估、行业弱势+标的高估、行业轮动转弱、数据严重缺失四类场景。

## 4. 建议的证据包结构

```yaml
identity:
  symbol / name / market / industry / data_source
rank_in_industry:
  pe_rank / pe_percentile / roe_rank / roe_percentile / valuation_label
value_score:
  score / value_ratio / pe_ratio / roe_ratio / interpretation
industry_trend:
  change_5d_pct / change_20d_pct / change_60d_pct / phase / signal / cycle
rotation_signals:
  signal_type / rotation_direction / strength / description
industry_chain:
  upstream / downstream / implication
catalysts:
  event / impact / target_month / months_until
data_quality:
  overall / has_constituents / has_trend / confidence_ceiling / notes
decision_matrix:
  relative_value_bucket / industry_trend_bucket / suggested_direction / reason
confidence_constraints:
  ceiling / max_confidence / hard_caps
```

## 5. 本轮执行计划

| 步骤 | 文件 |
| --- | --- |
| 生成行业证据包与矩阵 | `src/agents/industry_analyst.py` |
| 把证据包注入 Step B Prompt | `src/agents/industry_analyst.py` |
| 校验问题写回结果并降权 | `src/agents/industry_analyst.py` |
| 输出结构化 `data_summary` | `src/agents/industry_analyst.py` |
| 固定四类离线样本 | `tests/fixtures/industry_scenarios.json` |
| 补 IndustryAnalyst 护栏测试 | `tests/test_industry_v3.py` |

## 6. 下一轮建议

本轮之后还值得继续做:

1. 让 Aggregator 像消费公司前景证据一样，直接消费行业证据包。
2. 在 `PredictionStore` 验证完成后，把行业对比分析师样本回写 `IndustryConfidenceCalibrator`。
3. 为港股/美股补更稳定的行业分类和同行估值样本。
4. 做行业对比校准报告，按行业、数据质量、估值分位展示命中率。
5. 增加行业历史快照，避免回测时使用未来行业成分和未来趋势。

## 7. 本轮已执行优化

本轮已经完成以下修改:

| 文件 | 修改 |
| --- | --- |
| `src/agents/industry_analyst.py` | 新增行业证据包，包含行业排名、性价比、趋势、轮动、产业链、催化剂、数据质量 |
| `src/agents/industry_analyst.py` | 新增行业相对位置-趋势决策矩阵，输出 `suggested_direction` 和矩阵原因 |
| `src/agents/industry_analyst.py` | Step B Prompt 注入代码计算的行业证据包，并增加硬性约束 |
| `src/agents/industry_analyst.py` | Step A JSON 解析失败时，使用代码证据生成保守预判 |
| `src/agents/industry_analyst.py` | 一致性校验改为返回问题列表，并写回 `risks/reasoning/status` |
| `src/agents/industry_analyst.py` | 新增行业证据硬约束，矩阵冲突或超过证据上限时自动降权 |
| `src/agents/industry_analyst.py` | 新增结构化 `data_summary`，供 API、历史记录和后续 Aggregator 使用 |
| `src/data/prediction_store.py` | 预测验证完成后，行业对比分析师样本会回写 `IndustryConfidenceCalibrator` |
| `src/utils/industry_calibrator.py` | 新增公开 `save()` 方法，支持显式持久化校准统计 |
| `tests/fixtures/industry_scenarios.json` | 新增四类固定离线样本 |
| `tests/test_industry_v3.py` | 新增 IndustryAnalyst 护栏测试 |
| `tests/test_phase3.py` | 新增行业对比分析师验证回写校准器测试 |

## 8. 验证结果

已运行:

```bash
pytest -q tests/test_industry_v3.py tests/test_phase3.py tests/test_phase1.py tests/test_result.py
```

结果:

```text
99 passed, 2 deselected
```

并已运行:

```bash
python3 -m py_compile src/agents/industry_analyst.py src/data/prediction_store.py src/utils/industry_calibrator.py
```

结果: 通过。

## 9. 本轮之后的下一步

行业对比分析师已经从“有工具层能力但输出不可控”，推进到“有行业证据包、矩阵、护栏、结构化摘要和验证回写”。下一轮最值得做的是让 Aggregator 直接消费行业证据包，并在最终报告里展示行业矩阵、相对估值、行业趋势和催化剂。
