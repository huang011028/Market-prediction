# 20260706 近期股价分析师优化建议

## 1. 当前状态

近期股价分析师已经具备较强的技术面基础能力:

- `PriceFetcher` 会生成行情数据、技术指标、近期趋势、盘中信号和 `technical_snapshot`。
- `technical_snapshot` 已经包含趋势、动量、量能、波动率、支撑压力、风险收益、技术置信度和证据列表。
- `TechnicalAnalyst` 会把技术证据包注入 prompt，并用 snapshot 约束 LLM 输出。
- 已有技术特征测试和星网锐捷专项测试覆盖部分边界。

但它和其他 4 个 Agent 的差异是: 技术证据还没有完全进入统一的 `structured_evidence` 通道，Aggregator 不能像消费基本面、行业、宏观、新闻证据一样稳定消费技术证据。

## 2. 主要问题

| 问题 | 影响 |
| --- | --- |
| `data_summary.evidence` 之前只是技术证据列表 | Aggregator 读不到 `decision_matrix` 和 `confidence_constraints` |
| 技术面没有统一矩阵 | 趋势、动量、量能、盘中、风险收益之间的关系不够显式 |
| 技术面未进入 Aggregator 结构化证据域 | 短期预测中最重要的技术面权重仍可能按 reasoning 直接采信 |
| 最终报告缺少裁决复核 | LLM 最终方向可能和结构化证据多数方向冲突 |

## 3. 本轮优化目标

1. **统一技术证据包**
   - 把 `technical_snapshot` 包装为和其他 Agent 一致的结构: `decision_matrix / confidence_constraints / evidence`。

2. **技术趋势-动量-量能矩阵**
   - 结合短期趋势、动量状态、量能确认、盘中状态、风险收益，生成 `suggested_direction`。

3. **Aggregator 消费技术证据**
   - 把“近期股价分析师”加入 `structured_evidence` 域，领域标记为“技术”。

4. **最终裁决闭环**
   - Aggregator 在 LLM 输出最终结论后，再根据结构化证据做一次复核。
   - 如果最终方向与结构化证据多数方向明显冲突，降为 `neutral`、降低置信度，并写入风险和分歧。
   - 如果最终置信度超过结构化证据整体上限，自动压低置信度。

## 4. 建议技术证据包结构

```yaml
identity:
  symbol / name / market / data_source
price_window:
  data_period / latest_date / trading_days / timeframe
price_summary:
  latest_close / change_5d_pct / change_20d_pct
data_quality:
  status / score / issues / trading_days
trend_regime:
  short_term / medium_term
momentum_signals:
  state / score / rsi / macd
volume_signals:
  price_up_volume_up / price_down_volume_up / volume_trend
support_resistance:
  nearest_support / nearest_resistance / distance_pct
risk_levels:
  stop_loss_reference / breakout_reference / risk_reward_ratio
intraday_signals:
  available / state / evidence
decision_matrix:
  trend_bucket / momentum_bucket / volume_bucket / risk_reward_bucket / suggested_direction / reason
confidence_constraints:
  max_confidence / technical_confidence / signal_consistency_score / hard_caps
evidence:
  bullish / bearish / neutral
```

## 5. 本轮已执行优化

| 文件 | 修改 |
| --- | --- |
| `src/agents/technical_analyst.py` | 新增统一技术证据包 `_build_evidence_packet()` |
| `src/agents/technical_analyst.py` | 新增技术趋势-动量-量能-风险收益矩阵 `_derive_technical_matrix()` |
| `src/agents/technical_analyst.py` | Prompt 中注入统一技术证据包 |
| `src/agents/technical_analyst.py` | `data_summary.evidence` 改为统一结构，旧证据列表保留在 `legacy_evidence` |
| `src/agents/technical_analyst.py` | 技术约束问题写入 `data_summary.consistency_issues` |
| `src/agents/aggregator.py` | 把“近期股价分析师”加入结构化证据域 |
| `src/agents/aggregator.py` | 新增最终裁决闭环 `_apply_final_decision_guardrails()` |
| `src/agents/aggregator.py` | 新增结构化证据方向审计 `_build_decision_audit()` |
| `src/core/result.py` | 最终报告结构化证据域加入“技术” |
| `tests/test_technical_agent_star_net.py` | 新增技术证据包字段测试 |
| `tests/test_phase1.py` | 新增技术结构化证据和最终裁决闭环测试 |

## 6. 验证结果

已运行:

```bash
python3 -m py_compile src/agents/technical_analyst.py src/agents/aggregator.py src/core/result.py
```

结果: 通过。

已运行:

```bash
pytest -q tests/test_technical_agent_star_net.py tests/test_technical_features_v2.py tests/test_phase1.py tests/test_result.py
```

结果:

```text
65 passed, 2 deselected
```

已运行全量测试:

```bash
pytest -q
```

结果:

```text
280 passed, 2 skipped, 12 deselected
```

已运行:

```bash
git diff --check
```

结果: 通过。

## 7. 下一轮建议

本轮后，最值得继续做的是:

1. 把技术面历史验证结果回写到专用技术校准器。
2. 为技术矩阵增加固定离线样本: 放量突破、缩量反弹、压力位前看涨、支撑位前看跌、盘中背离。
3. 让 Aggregator 输出更清晰的“最终裁决审计表”，展示每个 Agent 的证据方向、调整后权重和置信上限。
4. 回测时强制技术面使用历史行情快照，避免任何未来数据泄漏。
