# 🎯 汇总分析师 — Round 1 深度优化设计文档

> **当前状态**：汇总分析师从 5 个分析 Agent 接收结果 → 构建上下文（含权重）→ LLM 综合研判 → 输出 FinalReport。
>
> **核心问题**：汇总 Agent 知道权重但不知道 Agent 质量。它不知道某个 Agent 被校准器降了置信度，不知道新闻源质量差，不知道技术面近期准确率更高。**它是一个盲眼的裁判。**

---

## 目录

1. [现状分析](#1-现状分析)
2. [优化一：Agent 质量感知](#2-优化一agent-质量感知)
3. [优化二：动态权重调整](#3-优化二动态权重调整)
4. [优化三：分场景预测输出](#4-优化三分场景预测输出)
5. [优化四：报告结构升级](#5-优化四报告结构升级)
6. [优化五：不确定性量化](#6-优化五不确定性量化)
7. [实施优先级与工作量](#7-实施优先级与工作量)

---

## 1. 现状分析

### 1.1 当前流程

```python
# aggregator.py 当前逻辑
async def aggregate(target, timeframe, agent_results, weight_config, failed_agents):
    context = self._build_context(...)   # 拼装各 Agent 结果 + 权重表
    response = await self.llm.achat(...)  # LLM 综合分析
    return self._parse_response(...)       # 解析 JSON → FinalReport
```

### 1.2 当前 Prompt 给 LLM 的信息

```
✅ 各 Agent 的方向、幅度、置信度、推理过程
✅ 固定权重表（来自 agent_config.yaml）
✅ 失败 Agent 列表
❌ 不知道哪些 Agent 的数据质量差
❌ 不知道技术面刚被校准器降了置信度
❌ 不知道新闻 Agent 的新闻源是"知识库"还是"实时"
❌ 不知道各 Agent 的历史准确率
```

### 1.3 典型问题案例

```
当前输出:
  技术面: bearish 60%  | 新闻: neutral 25%(知识库) | 基本面: neutral 10%(数据缺失)
  
  汇总 LLM 看到的:
    → 三方都是"有方向的判断"
    → 按权重 30%+20%+12% 加权
    → 输出: bearish 55%

  问题:
    新闻 25% 是基于知识库而非实时数据，置信度虚高？
    基本面 10% 基本等于弃权，但按权重算了
    技术面 60% 被校准器降过吗？如果校准后是 52%，汇总也不知道
```

---

## 2. 优化一：Agent 质量感知

### 2.1 核心思路

在构建 summary context 时，不只给 LLM 各 Agent 的结论，还给**质量标签**。

### 2.2 质量评分维度

从 `AnalysisResult` 中提取：

```python
class AgentQualityScorer:
    """Agent 分析质量评分"""
    
    def score(self, result: AnalysisResult, agent_name: str) -> dict:
        """
        返回:
        {
            "score": 0.75,           # 综合质量分 0~1
            "data_quality": "good",   # good / normal / partial / poor
            "source_type": "realtime", # realtime / reference / knowledge_base
            "calibration_note": "置信度从68%校准至62%",
            "issues": ["财务数据缺失", "估值指标不可用"],
            "is_reliable": True       # 是否值得参考
        }
        """
```

### 2.3 质量维度详解

| 维度 | 来源 | 判断规则 |
|------|------|---------|
| **数据质量** | `data_summary` / 推理文本 | "N/A"出现次数 > 5 → poor |
| **数据源类型** | `data_summary` / fetcher 标记 | tencent/sina → realtime; yfinance → partial; 知识库 → knowledge_base |
| **校准信息** | `data_summary.calibration` | 如果被校准过，提取原始vs校准后置信度 |
| **关键字段缺失** | 推理文本 | 统计"缺失"、"不可用"、"N/A"出现次数 |
| **有效推理长度** | `reasoning` 长度 | < 100 字 → 推理不充分 |

### 2.4 注入 Prompt

```markdown
## 🔍 Agent 分析质量评估

| Agent | 数据质量 | 数据源 | 可信度 | 备注 |
|-------|---------|--------|--------|------|
| 近期股价 | ✅ good | 腾讯K线+akshare | 高 | 置信度已校准:68%→62% |
| 最新新闻 | ⚠️ partial | 知识库兜底 | 低 | 无实时新闻，仅知识库分析 |
| 公司前景 | ⚠️ partial | 腾讯PE+akshare | 中 | 财务数据缺失，估值数据可用 |
| 国际形势 | ✅ good | 东方财富+新浪 | 中高 | CPI/PMI/GDP实时, 部分参考值 |
| 行业对比 | ⚠️ partial | 参考值+腾讯PE | 中 | 行业均值不可用 |

💡 提示: 质量低的 Agent 应降低其在综合判断中的实际权重。
```

---

## 3. 优化二：动态权重调整

### 3.1 当前问题

`agent_config.yaml` 的权重是死的。不管新闻 Agent 是"实时 10 条新闻"还是"知识库兜底"，权重都是 20%。

### 3.2 动态调整规则

```python
class DynamicWeightAdjuster:
    """动态权重调整器"""
    
    def adjust(self, weight_config, quality_scores, timeframe):
        """
        输入: YAML 静态权重 + 各 Agent 质量评分
        输出: 调整后的权重
        
        调整规则:
        1. 数据质量降权:
           poor: 权重 × 0.3
           partial: 权重 × 0.6
           
        2. 置信度极低降权:
           置信度 < 0.15: 权重 × 0.2（基本等于弃权）
           置信度 < 0.25: 权重 × 0.5
           
        3. 数据源类型降权:
           knowledge_base: 权重 × 0.5（知识库分析可靠度减半）
           
        4. 重新归一化: 降到 0 的权重按比例分配给 quality="good" 的 Agent
        """
```

### 3.3 效果示例

```
静态权重（agent_config.yaml）:
  技术面 30% + 新闻 20% + 基本面 12% + 宏观 12% + 行业 10%

动态调整后（假设新闻质量 poor + 基本面数据部分缺失）:
  技术面 38% + 新闻 8% + 基本面 7% + 宏观 18% + 行业 12%
  ↑ 质量高的提权，质量低的降权
```

### 3.4 注入 Prompt

```markdown
## ⚖️ 权重调整说明

| Agent | 静态权重 | 调整原因 | 动态权重 |
|-------|---------|---------|---------|
| 近期股价 | 30% | 数据质量good, 无调整 | **30%** |
| 最新新闻 | 20% | 知识库兜底(×0.5) + 置信度低(×0.5) | **5%** |
| 公司前景 | 12% | 财务数据缺失(×0.6) | **7%** |
| 国际形势 | 12% | 无调整 | **12%** |
| 行业对比 | 10% | 行业数据缺失(×0.6) | **6%** |

> 动态权重仅供参考，你作为研究主管可以根据分析质量进一步调整。
```

---

## 4. 优化三：分场景预测输出

### 4.1 当前问题

汇总只输出一个方向+一个幅度+一个置信度，但实际市场有多种可能路径。

### 4.2 多场景输出

```markdown
## 🔮 分场景预测

### 最可能场景（概率 ~55%）
- 方向: 📉 看跌
- 幅度: -4% ~ +1%
- 逻辑: 技术面空头排列 + 基本面无催化剂 → 惯性下探支撑

### 乐观场景（概率 ~25%）  
- 方向: 📈 看涨
- 幅度: +1% ~ +5%
- 触发条件: 宏观利好（如降准）或北向资金大幅流入

### 悲观场景（概率 ~20%）
- 方向: 📉 看跌
- 幅度: -8% ~ -4%
- 触发条件: 放量跌破关键支撑 9.99 元
```

### 4.3 实现

在 `FinalReport` 中新增字段，在 Aggregator Prompt 中要求 LLM 输出：

```python
@dataclass  
class FinalReport:
    # ... 原有字段 ...
    
    # 🆕 Round1
    scenarios: list[dict] = field(default_factory=list)
    # [
    #   {"label": "最可能", "probability": 0.55, "direction": "bearish", ...},
    #   {"label": "乐观", "probability": 0.25, "direction": "bullish", ...},
    #   {"label": "悲观", "probability": 0.20, "direction": "bearish", ...},
    # ]
```

---

## 5. 优化四：报告结构升级

### 5.1 当前 Markdown 结构

```
综合预测 → 各方观点摘要(LLM自由发挥) → 四维交叉验证 → 加权综合 → 最终结论
```

问题：LLM 自由发挥导致格式不统一，有时非常冗长。

### 5.2 升级后结构

```markdown
# 📊 市场预测分析报告

## 🎯 综合预测（一句话）
看跌，-3%~+1%，置信度 55%

## 📊 各维度置信度（表格，代码生成）
| Agent | 方向 | 幅度 | 置信度 | 质量 |
|-------|------|------|--------|------|
| ...   |      |      |        | ✅/⚠️/❌ |

## 🔮 分场景预测
（三个场景卡片）

## 📋 各方核心观点（每个 Agent 1-2句话）
- 技术面: ...
- 新闻面: ...
- ...

## 🔗 交叉验证矩阵（表格化）
| 交叉对 | 一致性 | 说明 |
|--------|--------|------|
| 技术×新闻 | ✅ 一致 | ... |
| 基本面×宏观 | ⚠️ 部分矛盾 | ... |
| 技术×基本面 | ❌ 矛盾 | ... |

## ⚠️ 关键风险（Top 5）

## 🔀 分歧点

## ⚖️ 权重说明（代码生成表格）
```

### 5.3 代码生成 vs LLM 生成

| 部分 | 生成方式 | 原因 |
|------|---------|------|
| 各维度置信度表格 | **代码生成** | 数据来自 AnalysisResult，不需要 LLM |
| 权重说明 | **代码生成** | 来自 WeightConfig + DynamicAdjuster |
| 质量评估表 | **代码生成** | 来自 AgentQualityScorer |
| 综合预测 | LLM | 需要判断 |
| 交叉验证 | LLM | 需要推理 |
| 分场景 | LLM | 需要推理 |
| 风险/分歧 | LLM | 需要推理 |

---

## 6. 优化五：不确定性量化

### 6.1 问题

当前只有一个置信度数字（如 55%）。但 55% 到底意味着什么？

### 6.2 置信度区间

```markdown
## 📐 置信度解读

综合置信度: 55% (中等)

置信区间: [40%, 65%]
- 下限 40%: 最保守估计（如果质量低的 Agent 警告成真）
- 上限 65%: 最乐观估计（如果技术面信号完全兑现）

不确定性来源:
- 新闻面数据缺失贡献 8% 不确定性
- 基本面财务数据缺失贡献 5% 不确定性
- Agent 间分歧贡献 7% 不确定性
```

### 6.3 实现

```python
def calculate_confidence_interval(agent_results, quality_scores, weight_config):
    """
    置信区间计算:
    1. 基准信心 = LLM 综合判断的置信度
    2. 下调因素 = Σ(低质量Agent权重 × 不确定性贡献)
    3. 上调因素 = 高质量Agent共识度奖励
    """
```

---

## 7. 实施优先级与工作量

```
优先级  优化项                        预期收益  工作量  改动文件
────── ──────────────────────────     ──────  ─────  ──────────────────
 ⭐⭐⭐  优化一: Agent质量感知            高       低     aggregator.py(新增scorer)
 ⭐⭐⭐  优化二: 动态权重调整             高       低     aggregator.py + weight_manager
 ⭐⭐    优化四: 报告结构升级             中       低     result.py(to_markdown升级)
 ⭐⭐    优化五: 不确定性量化             中       中     aggregator.py
 ⭐      优化三: 分场景预测              中       中     result.py + aggregator prompt

建议路线:
  Day 1: 优化一 + 优化二 → 质量感知+动态权重，核心提升
  Day 2: 优化四 → 报告结构，体验提升
  Day 3: 优化五 + 优化三 → 锦上添花
```

---

> 📌 **汇总分析师是"裁判"**。Round 1 的核心是让它从"盲眼裁判"变成"能看到每个选手状态的裁判"——知道谁的数据好、谁的置信度被校准过、谁的判断更可信，从而做出更精准的综合判断。
