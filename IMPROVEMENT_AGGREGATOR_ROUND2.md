# 🎯 汇总分析师 — Round 2 深度优化设计文档

> **Round 1 回顾**：实现了 Agent 质量感知和动态权重调整。227 个测试全过。汇总 Analyst 现在能自动识别哪些 Agent 数据质量差、基于知识库、置信度极低，并据此调整权重。
>
> **Round 2 核心命题**：从"质量感知"升级到"可解释决策"——让汇总的理由更透明、更结构化、更能指导后续改进。

---

## 1. Round 1 实际交付 vs 计划

| 计划 | 状态 | 效果 |
|------|------|------|
| ⭐⭐⭐ Agent 质量感知 | ✅ 完成 | 自动标记 poor/partial/good + 知识库/实时 |
| ⭐⭐⭐ 动态权重调整 | ✅ 完成 | poor→×0.3, partial→×0.6, 知识库→×0.5 |
| ⭐⭐ 报告结构升级 | 📅 推迟 | 优先级下调 |
| ⭐⭐ 不确定性量化 | 📅 推迟 | 需更多数据 |
| ⭐ 分场景预测 | 📅 推迟 | 单独评估 |

### Round 1 工作机制

```
每个 Agent 的 AnalysisResult → _score_quality()
  ├─ 统计 N/A/缺失/不可用次数 → data_quality (good/normal/partial/poor)
  ├─ 检测 "知识库"/"参考值"  → source_type
  └─ 综合 → reliability (high/medium/low)
        ↓
  _adjust_weights()
  ├─ poor → 权重 × 0.3
  ├─ partial → 权重 × 0.6  
  ├─ knowledge_base → 权重 × 0.5
  └─ 重新归一化
        ↓
  _build_context() → LLM 收到质量评估表 + 调整后权重
```

---

## 2. Round 2 目标清单

| # | 优化 | 产出 | 优先级 |
|---|------|------|--------|
| 2.1 | 决策可解释性增强 | `aggregator.py` 升级 | ⭐⭐⭐ |
| 2.2 | 分歧量化引擎 | `aggregator.py` 新增 | ⭐⭐⭐ |
| 2.3 | 汇总准确率自追踪 | `aggregator.py` + `prediction_store` | ⭐⭐ |
| 2.4 | 报告结构化升级 | `result.py` to_markdown | ⭐⭐ |
| 2.5 | Agent 贡献度透明化 | `aggregator.py` | ⭐ |

---

## 3. 优化一：决策可解释性增强

### 3.1 当前问题

汇总的 `summary` 是 LLM 自由发挥的一大段文字。用户要读完 500 字才知道为什么是这个方向。无法快速理解决策逻辑。

### 3.2 方案：决策卡片

在 `to_markdown` 开头增加"决策卡片"：

```markdown
## 🎯 综合预测

### 📋 决策卡片
┌─────────────────────────────────────────┐
│ 方向: 📉 看跌                            │
│ 幅度: -3.0% ~ +1.0%                     │
│ 置信度: 55% (中等)                       │
│                                         │
│ 主导信号: 技术面(权重38%) + 宏观(权重18%)  │
│ 质量警告: 新闻知识库兜底(权重仅5%)         │
│ 核心矛盾: 技术面空头 vs 宏观估值支撑       │
│ 推翻条件: 放量突破MA20(10.61)             │
└─────────────────────────────────────────┘
```

### 3.3 实现

```python
# FinalReport 新增字段
@dataclass
class FinalReport:
    # ... 原有字段 ...
    
    # 🆕 Round2
    decision_card: dict = field(default_factory=dict)
    # {
    #     "dominant_signals": ["技术面看跌(权重38%)", "宏观偏多(权重18%)"],
    #     "quality_warnings": ["新闻知识库兜底"],
    #     "core_tension": "技术面空头 vs 宏观估值支撑",
    #     "invalidation_condition": "放量突破MA20(10.61)",
    # }
```

---

## 4. 优化二：分歧量化引擎

### 4.1 当前问题

汇总 LLM 在 `disagreements` 字段描述分歧，但这是文字描述，没有量化。无法快速回答"Agent 之间有多不一致？"

### 4.2 量化方案

```python
class DisagreementCalculator:
    """分歧量化"""
    
    def calculate(self, agent_results):
        """
        返回：
        {
            "level": "low",               # low/medium/high/severe
            "direction_entropy": 0.45,     # 方向熵（0=完全一致, 1=完全随机）
            "magnitude_spread": 8.0,       # 最大幅度差
            "confidence_variance": 0.15,   # 置信度方差
            "clusters": {
                "bearish": ["技术面", "宏观"],
                "neutral": ["新闻", "基本面"],
                "bullish": []
            },
            "outlier": "国际形势"  # 如果有明显与其他人不同的 Agent
        }
        """
```

### 4.3 分歧矩阵

```markdown
## 🔀 分歧矩阵

| Agent A | Agent B | 方向一致 | 幅度重叠 | 分歧程度 |
|---------|---------|---------|---------|---------|
| 技术面(bearish) | 新闻(neutral) | ❌ | 部分 | 中等 |
| 技术面(bearish) | 基本面(neutral) | ❌ | 部分 | 中等 |
| 国际形势(bullish) | 技术面(bearish) | ❌ | 否 | **严重** |
```

### 4.4 注入 Prompt

```markdown
## 📐 分歧量化

- 分歧等级: **高** (3个不同方向)
- 方向分布: 看跌2 / 中性2 / 看涨1
- 最一致组合: 技术面+宏观 → 看跌
- 最大分歧: 国际形势(看涨) vs 技术面(看跌) — 幅度区间完全不重叠
- ⚠️ 异常值: 国际形势观点与其他 Agent 差距最大
```

---

## 5. 优化三：汇总准确率自追踪

### 5.1 问题

汇总 Analyst 不知道自己的历史准确率如何。如果它知道"上次类似的多空分歧情况下，我判断看跌的准确率是 60%"，会做出更好的判断。

### 5.2 方案

在 `_build_context` 中注入汇总 Agent 自身的历史统计：

```python
def _get_self_stats(self, timeframe):
    stats = self.store.get_accuracy_stats(agent_name=None, timeframe=timeframe)
    # agent_name=None 查的是综合汇总
    if stats["total"] >= 5:
        return f"历史准确率: 方向{stats['direction_accuracy']:.0%}, 幅度{stats['magnitude_accuracy']:.0%} ({stats['total']}次)"
    return ""
```

注入 Prompt：

```markdown
## 📊 汇总历史表现
- 短期预测: 方向准确率 62% (21次验证)
- 同类型情况(多方分歧): 方向准确率 55% (8次)
- 平均置信度 58% vs 实际准确率 62% → 轻微保守，可适当提高信心
```

---

## 6. 优化四：报告结构化升级

### 6.1 目标

让 `FinalReport.to_markdown()` 输出更清晰、结构化的报告。

### 6.2 改进点

| 部分 | 当前 | Round2 |
|------|------|--------|
| 综合预测 | 列表 | **决策卡片 + 列表** |
| 各维度置信度 | 表格 | 表格 + 质量标签 |
| AI 分析 | LLM 自由文本 | **分节标题引导 LLM** |
| 权重 | 静态列表 | **动态权重对比表**（静态→调整后） |
| 分歧 | 文字 | **分歧矩阵 + 文字** |
| 来源 | 无 | **数据源说明** |

### 6.3 数据源说明

```markdown
## 📡 数据来源

| 数据 | 来源 | 状态 |
|------|------|------|
| K线数据 | 腾讯行情API | ✅ 实时 |
| 财务数据 | akshare 财报 | ✅ 2026Q1 |
| 宏观CPI/PMI/GDP | 东方财富DC | ✅ 实时 |
| 汇率 | 新浪行情 | ✅ 实时 |
| PE/PB | 腾讯行情 | ✅ 实时 |
| 港股新闻 | 东方财富 | ⚠️ 部分 |
```

---

## 7. 优化五：Agent 贡献度透明化

### 7.1 问题

最终方向是汇总 LLM 综合判断的结果，但用户不知道每个 Agent 对这个结论"贡献"了多少。

### 7.2 方案

```markdown
## 🏷️ Agent 贡献度

| Agent | 静态权重 | 质量调整 | 实际影响 | 对结论的贡献 |
|-------|---------|---------|---------|------------|
| 近期股价 | 30% | 无 | **主导** | "提供了空头排列的核心看跌信号" |
| 最新新闻 | 20% | ×0.5(知识库) | **微弱** | "仅确认无突发利好" |
| 公司前景 | 12% | ×0.6(数据缺) | **参考** | "确认估值有支撑，但不驱涨" |
| 国际形势 | 12% | 无 | **重要** | "宏观宽松提供下档保护" |
| 行业对比 | 10% | ×0.6(数据缺) | **微弱** | "行业数据不足，仅参考" |
```

---

## 8. 实施优先级与工作量

```
优先级  优化项                          预期收益  工作量  依赖
────── ──────────────────────────────── ──────  ─────  ────────
 ⭐⭐⭐  优化一: 决策可解释性              中       低     无
 ⭐⭐⭐  优化二: 分歧量化引擎              高       中     无
 ⭐⭐    优化四: 报告结构化升级             中       中     to_markdown
 ⭐⭐    优化五: Agent贡献度透明化          中       低     无
 ⭐      优化三: 汇总自追踪                中       低     PredictionStore

建议路线:
  Day 1: 优化二 → 分歧量化，最高频痛点
  Day 2: 优化一 + 优化五 → 决策卡片 + 贡献度表
  Day 3: 优化四 → 报告结构化
```

---

## 附录：汇总 Analyst 进化路线图

```
Phase 1/2 (最初):
  收集 Agent 结果 → 简单加权 → LLM 自由发挥 → 输出

Round 1 (已完成):
  收集 Agent 结果
    → [质量评分] 自动评估数据质量 + 数据源类型 + 可信度
    → [动态权重] poor×0.3, partial×0.6, 知识库×0.5 + 归一化
    → LLM 基于质量表 + 动态权重综合研判
    → 输出

Round 2 (本阶段):
  收集 Agent 结果
    → [质量评分 + 动态权重] (Round1)
    → [分歧量化] 计算方向熵 + 分歧矩阵 + 异常值检测
    → [决策卡片] 提取主导信号、核心矛盾、推翻条件
    → [贡献度] 计算每个 Agent 的实际贡献
    → LLM 基于全量结构化信息综合研判
    → [结构化报告] 决策卡片 + 质量表 + 贡献度 + 分歧矩阵
    → 输出
```

---

> 📌 **Round 2 的核心思想**：从"内部质量感知"走向"外部可解释决策"。Round 1 让汇总知道自己更该听谁的；Round 2 让它能向用户解释"为什么是这个结论、各 Agent 贡献了多少、分歧有多大"。
