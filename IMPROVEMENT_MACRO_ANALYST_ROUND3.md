# 🌍 国际形势分析师改进方案 — Round 3

> **版本**: v1.0 | **日期**: 2026-07-03 | **前置**: ROUND1 ✅ | ROUND2 ✅

---

## 1. Round 1-2 累计成果

| Round | 核心交付 |
|-------|---------|
| **Round 1** | LPR/M2实时化、DXY/VIX/US10Y/Fed多源尝试、数据新鲜度评分、12行业宏观敏感因子映射(`stock_context.py`)、两步CoT、Few-shot+市场区分+置信度锚定、一致性校验 |
| **Round 2** | FRED API 接入（**全部11项指标实时化，新鲜度100%**）、地缘政治事件实时采集(`geopolitical_fetcher.py`)、置信度校准器集成 |

### 当前架构全景

```
MacroFetcherV2 (FRED+东方财富+Sina+akshare — 11/11实时)
       │
       ├── stock_context (行业→敏感因子→传导链)
       ├── geopolitical_fetcher (地缘事件采集)
       │
       ▼
MacroAnalyst (两步CoT: 宏观评估 → 标的传导)
       │
       ▼
校验 + 置信度校准 → AnalysisResult
```

---

## 2. Round 2 遗留项 & Round 3 方向

### 2.1 未完全实现的 Round 2 项

| 项目 | 状态 | Round 3 优先级 |
|------|------|---------------|
| yfinance 重试优化 | 已添加 FRED 作为 VIX 主源，yfinance 降级 | ✅ 已解决 |
| 宏观因子重要性追踪 | 未实现（需积累足够回测数据） | 🟡 中 |
| 源权重自动更新集成 | `source_weight_manager.py` 已创建，未接入 macro_analyst | 🟢 低 |

### 2.2 Round 3 核心主题：**从"数据全"到"判断准"**

当前宏观分析师的**数据管道已完美**（11/11实时），瓶颈在于：
1. 宏观判断本身的不确定性管理
2. 跨资产类别的宏观传导准确性
3. 宏观分歧场景的处理

---

## 3. Round 3 改进方向

### 3.1 宏观场景化 Prompt 🔴 高

**问题**: 当前 prompt 覆盖了常规宏观分析框架，但没有针对特定宏观场景做专项优化。

**方案**: 增加场景化分析指引

```python
# 场景1: Fed 政策拐点期
FED_PIVOT_GUIDANCE = """
当检测到 Fed 可能转向时（利率在高位+市场定价降息）：
- 这是宏观交易中最重要的时刻——方向判断的置信度可以比平时高 5-10%
- 关注点不在"会不会降息"（市场已定价）而在"降息节奏和幅度"
- 利率敏感行业（互联网/地产）的弹性远大于利率不敏感行业（公用事业）
"""

# 场景2: 滞胀风险
STAGFLATION_GUIDANCE = """
当 CPI 仍在高位但 PMI 跌破 50：
- 滞胀是最难做宏观判断的环境——方向应以 neutral 为主
- 防御性行业（医药/公用事业）相对收益但绝对收益也有限
- 不要给出过高的 confidence（上限 0.55）
"""

# 场景3: 人民币急贬/急升
RMB_SHOCK_GUIDANCE = """
当人民币单日波动 > 0.5%：
- 急贬 → 港股资金外流加速 → 短期回避港股
- 急升 → 外资回流 → 港股权重股率先受益
- 急升急贬后通常有技术性回调 → 不要做趋势外推
"""

# 场景4: VIX 飙升（>25）
VIX_SPIKE_GUIDANCE = """
当 VIX > 25：
- 市场进入恐慌模式，宏观基本面暂时失效
- 所有风险资产同跌（相关性→1），行业区分不重要
- 不要试图"抄底"——等待 VIX 回落至 20 以下再恢复正常分析框架
"""
```

### 3.2 跨资产宏观传导矩阵 🟡 中

**问题**: 当前 `stock_context.py` 的行业→宏观敏感因子是静态的。实际上，同一行业内的不同公司（如大市值 vs 小市值、高杠杆 vs 低杠杆）对同一宏观因子的反应差异很大。

**方案**: 增加公司级差异因子

```python
# stock_context.py 扩展
COMPANY_MACRO_MODIFIERS = {
    # 市值修正：大盘股对利率更敏感（机构持仓多，DCF估值主导）
    "large_cap": {"rate_sensitive_mod": +0.1, "liquidity_sensitive_mod": +0.1},
    "small_cap": {"rate_sensitive_mod": -0.1, "liquidity_sensitive_mod": -0.1},
    
    # 杠杆修正：高杠杆对利率更敏感
    "high_leverage": {"rate_sensitive_mod": +0.15},
    "low_leverage": {"rate_sensitive_mod": -0.1},
    
    # 跨境业务修正
    "high_overseas_revenue": {"fx_sensitive_mod": +0.2, "geopolitical_sensitive_mod": +0.1},
    "domestic_only": {"fx_sensitive_mod": -0.1},
}
```

### 3.3 宏观预测自评机制 🟡 中

**方案**: 在 CoT 末端增加"宏观预测置信度自评"

```
Step 2 结束后，追加自评问题：

1. 本次判断最依赖哪个宏观因子？（如"主要依赖 Fed 降息预期"）
2. 如果这个因子被证伪（如 Fed 不降息），方向会反转吗？
3. 历史上有多少比例的情况下，类似的宏观组合导致了预测的方向？
4. 数据质量是否支持当前的置信度水平？

→ 输出 self_assessment 字段
→ aggregator 可用此信息做二次加权
```

### 3.4 宏观分歧信号 🟢 低

多个宏观因子指向不同方向时（如 Fed 降息利好 vs PMI 下行利空），明确标注分歧并给出"情景分析"而非单一方向。

---

## 4. 实施路线图（Round 3）

| 优先级 | 任务 | 工作量 | 预期收益 |
|--------|------|--------|---------|
| 🔴 P0 | 宏观场景化 Prompt（4个高频场景） | 0.5 天 | 特殊市场环境下的判断稳定性 ↑ |
| 🟡 P1 | 跨资产传导矩阵（公司级差异因子） | 1 天 | 宏观→标的传导精度 ↑ |
| 🟡 P1 | 宏观预测自评机制 | 0.5 天 | Aggregator 可二次加权 |
| 🟡 P1 | 宏观因子重要性追踪（回测分析） | 1.5 天 | 数据驱动发现哪些指标最有预测力 |
| 🟢 P2 | 历史相似宏观环境 RAG | 1.5 天 | 类比历史上类似的宏观组合 |
| 🟢 P2 | 多市场宏观对比仪表盘 | 1 天 | 同时展示中美宏观分歧 |

---

## 5. 从 Round 1 到 Round 3 的进化总结

| 维度 | Round 1 前 | Round 1 后 | Round 2 后 | Round 3 目标 |
|------|-----------|-----------|-----------|------------|
| 数据实时率 | 35% (5/14) | 45% (7/11) | **100% (11/11)** 🎉 | 保持 |
| 行业敏感度 | ❌ 无 | ✅ 12行业静态映射 | ✅ | 公司级动态修正 |
| 地缘事件 | LLM记忆 | LLM记忆 | ✅ 实时采集 | 保持 |
| 推理架构 | 单pass | 两步CoT | 两步CoT | +场景化+自评 |
| 校准 | ❌ 无 | 规则校验 | 规则+历史 | 宏观专用校准 |
| 特殊场景 | ❌ | ❌ | ❌ | 4个高频场景指引 |
| 样本积累 | 0 | 少量 | 累积中 | 因子重要性分析 |

---

> 📌 **Round 3 核心**: 数据全了（100% 实时），现在是让宏观分析在特殊场景（Fed拐点、滞胀、VIX飙升、汇率急变）下也能给出稳定、诚实的判断。同时从"泛行业敏感度"升级为"公司级敏感度"。
