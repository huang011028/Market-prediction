# 📊 近期股价分析师 — 深度优化设计文档

> **目标**：将当前"能用"的技术面分析师，升级为"专业级"的多维度、自校准、可进化的技术分析系统。
>
> **原则**：每个优化可独立实施、独立测试；优先低成本的 Prompt 和数据优化，再做大架构改动。

---

## 目录

1. [现状分析](#1-现状分析)
2. [优化路线图](#2-优化路线图)
3. [第一层：数据源增强](#3-第一层数据源增强)
4. [第二层：分析流程升级](#4-第二层分析流程升级)
5. [第三层：Prompt 精炼](#5-第三层prompt-精炼)
6. [第四层：质量校准](#6-第四层质量校准)
7. [第五层：自进化机制](#7-第五层自进化机制)
8. [实施顺序建议](#8-实施顺序建议)

---

## 1. 现状分析

### 1.1 当前能做到的

```
输入: 000001, 短期(1周)
  ↓
PriceFetcher → 腾讯/akshare 取3个月日K线 → pandas 计算:
  MA(5/10/20/60) + MACD(12,26,9) + RSI(14) + BOLL(20,2) + VOL_ratio
  ↓
BaseAgent._build_user_prompt → JSON 数据 → LLM 一次性分析
  ↓
输出: direction + magnitude + confidence + reasoning
```

### 1.2 当前不足

| 维度 | 问题 | 影响 |
|------|------|------|
| **指标覆盖** | 只有 4 类基础指标 | 无法识别成交量背离、趋势强度、波动率变化 |
| **多周期** | 只看日线 | 无法判断短期反弹 vs 中期趋势反转 |
| **形态识别** | 仅文字描述 MA排列/RSI区间 | 缺具体形态（头肩顶、双底、三角形突破） |
| **Prompt** | 通用金融分析框架 | 未针对技术分析优化，缺乏案例引导 |
| **置信度** | LLM 主观判断 | 无客观校准，有时过度自信/不自信 |
| **自进化** | 完全没有 | 每次分析独立，不利用历史验证结果 |
| **异常处理** | 仅兜底 neutral | 停牌、涨跌停、除权除息未特殊处理 |

### 1.3 近期股价分析师在团队中的定位

```
短期(1周)权重 30% → 团队中最高 → 它的质量直接影响最终预测
中期(1月)权重 20% → 仅次于基本面
长期(1季)权重 8%  → 让位给基本面/宏观
```

> **结论**：短期预测是近期股价分析师的主战场，优化重心应放在**短期准确率提升**。

---

## 2. 优化路线图

```
第一层: 数据源增强 ─────── 投入产出比最高，立即见效
  ├── 新增技术指标 (ATR, KDJ, OBV, ADX)
  ├── 多周期数据 (周线/月线背景)
  └── 板块指数数据

第二层: 分析流程升级 ───── 中等投入，显著提升
  ├── 两阶段分析（信号检测 → 综合研判）
  ├── 指标共振评分
  └── 历史相似形态匹配

第三层: Prompt 精炼 ─────── 零成本，快速迭代
  ├── 加入分析框架引导
  ├── 异常情况处理模板
  └── 输出格式强化

第四层: 质量校准 ───────── 需要历史数据积累
  ├── 置信度校准
  ├── 幅度准确性评估
  └── 多空偏好修正

第五层: 自进化机制 ─────── 需要最多数据，但最具长期价值
  ├── 预测事后分析
  ├── 指标权重自适应
  └── 标的个性化参数
```

---

## 3. 第一层：数据源增强

### 3.1 新增技术指标

当前 4 类指标 → 扩展为 **8 类指标**：

| 类别 | 指标 | 代码 | 价值 |
|------|------|------|------|
| **趋势** | MA(5/10/20/60/120/250) | 已有，扩展周期 | 增加半年线/年线判断长期趋势 |
| **动能** | MACD(12,26,9) | 已有 | — |
| | KDJ(9,3,3) | 🆕 | 更敏感的超买超卖，A股常用 |
| | RSI(14) | 已有 | — |
| **成交量** | VOL_ratio | 已有 | — |
| | **OBV (能量潮)** | 🆕 | 量价背离检测 |
| | **VWAP (均价)** | 🆕 | 机构成本参考 |
| **波动** | BOLL(20,2) | 已有 | — |
| | **ATR(14) (真实波幅)** | 🆕 | 量化波动程度，辅助幅度预测 |
| **趋势强度** | **ADX(14)** | 🆕 | >25有趋势，<20震荡，区分真假突破 |
| **支撑阻力** | 20日高低点 | 已有 | — |
| | **Pivot Points** | 🆕 | 经典支撑/阻力位计算 |
| **市场宽度** | **板块指数涨跌** | 🆕 | 个股 vs 板块相对强弱 |
| **形态** | 均线排列/RSI区间 | 已有 | — |
| | **K线形态枚举** | 🆕 | 锤子线、吞没、十字星等 |

### 3.2 多周期数据

```
当前: 仅日线
优化: 日线(主力) + 周线(中期背景) + 60分钟线(精细入场)

周线数据用于:
  - 判断中期趋势方向（与日线是否一致？）
  - 确认日线信号的有效性
  - 例: 日线看涨 + 周线空头 → 短期反弹而非趋势反转

60分钟线用于:
  - 短期(1-3天)精确预测
  - 识别盘中关键价位
```

### 3.3 板块指数数据

```
当前: 不看板块
优化: 获取标的所属板块指数同期走势

价值:
  - 个股弱于板块 → 跑输，需警惕
  - 个股强于板块 → 龙头，趋势延续概率大
  - 板块整体走强 + 个股技术面好 → 双保险信号
```

### 3.4 实现方案

```python
# price_fetcher.py 新增方法

class PriceFetcher:
    # --- 新增指标计算 ---
    
    def _compute_volume_indicators(self, df) -> dict:
        """OBV, VWAP"""
        ...
    
    def _compute_volatility_indicators(self, df) -> dict:
        """ATR(14)"""
        ...
    
    def _compute_trend_strength(self, df) -> dict:
        """ADX(14) + +DI/-DI"""
        ...
    
    def _compute_kdj(self, df) -> dict:
        """KDJ(9,3,3)"""
        ...
    
    def _compute_pivot_points(self, df) -> dict:
        """经典枢轴点"""
        ...
    
    def _detect_candlestick_patterns(self, df) -> list[str]:
        """识别最近5根K线的经典形态"""
        ...
    
    # --- 多周期数据 ---
    
    async def fetch_multi_timeframe(self, symbol, periods=["3mo", "6mo"]) -> dict:
        """获取日线 + 周线 + 60分钟线"""
        ...
    
    # --- 板块数据 ---
    
    async def fetch_sector_data(self, symbol) -> dict:
        """获取板块指数 + 对比"""
        ...
```

---

## 4. 第二层：分析流程升级

### 4.1 当前流程 vs 优化流程

```
当前:  数据 → LLM一次性分析 → 输出

优化:  数据 → 指标共振评分 → 两阶段LLM分析 → 校准 → 输出
              ↓
         检测矛盾信号
         量化多空强度
```

### 4.2 两阶段 LLM 分析

**阶段1: 信号检测（客观识别）**

让 LLM 先不急着判断方向，而是系统性地识别技术信号：

```
Prompt: "请逐一检查以下技术信号，只输出信号清单，不做方向判断：

### 趋势信号
☐ MA5上穿MA10 (金叉) → ____
☐ MA5下穿MA10 (死叉) → ____
☐ MA多头排列 → ____
☐ MA空头排列 → ____

### 动能信号
☐ MACD金叉 → ____
☐ MACD死叉 → ____
☐ RSI超买(>70) → ____
☐ RSI超卖(<30) → ____

### 成交量信号
☐ 放量上涨 → ____
☐ 缩量下跌 → ____
☐ OBV与价格背离 → ____

### 形态信号
☐ K线反转形态: ____
☐ 突破形态: ____

### 多周期验证
☐ 日线与周线方向一致 → ____
☐ 日线与周线方向背离 → ____
"

输出: 结构化的信号清单
```

**阶段2: 综合研判（主观评估）**

拿到信号清单后，LLM 做综合判断：

```
Prompt: "基于上面的信号清单：

1. 看涨信号和看跌信号各有哪些？哪个更强？
2. 信号之间是否有共振？（多指标同时指向同一方向）
3. 是否有背离？（价格与指标方向不一致）
4. 多周期是否确认？（日线与周线/60分钟线方向一致）
5. 综合判断: 方向 + 幅度 + 置信度"
```

### 4.3 指标共振评分

在 LLM 分析之前，先做一个量化的"多空强度评分"：

```python
class SignalScorer:
    """技术信号共振评分器"""
    
    def score(self, indicators: dict) -> dict:
        """
        为每个方向计算客观分数
        
        Returns:
            {
                "bullish_score": 65,    # 看涨信号强度 0-100
                "bearish_score": 35,    # 看跌信号强度 0-100
                "neutral_score": 20,    # 矛盾/中性
                "dominant": "bullish",
                "confidence_anchor": 0.65,  # 给LLM的置信度参考
                "signal_details": {
                    "trend": "slightly_bullish",
                    "momentum": "neutral",
                    "volume": "bearish_divergence",
                    "volatility": "contracting",
                }
            }
        """
```

这个评分不会替代 LLM，而是作为 LLM 分析的**输入参考**——LLM 可以看到"根据客观规则，看涨信号更强"但仍然可以根据经验推翻这个判断（比如识别了特殊形态）。

### 4.4 历史相似形态匹配

利用 Phase 3 的 `PredictionStore` 和 RAG 能力：

```
当前标的的技术特征 → 向量化 → 检索历史上最相似的 N 个技术面案例
  → 查看这些案例的事后验证结果
  → 作为 LLM 分析的参考
```

例如：
```
"历史上与当前技术形态最相似的3次情况：
  1. 2026-03-15: MA空头+MACD死叉+RSI超卖 → 预测看跌 → 实际下跌1.8% ✓
  2. 2026-01-20: 类似形态 → 预测看跌 → 实际反弹0.5% ✗ (出现了金叉反转)
  3. 2025-11-08: 类似形态 → 预测看跌 → 实际下跌3.2% ✓
  
  参考：此形态历史上看跌准确率 67%"
```

---

## 5. 第三层：Prompt 精炼

### 5.1 当前 Prompt 的问题

```python
# 当前: 笼统的分析框架
"1. 趋势判断 2. 动能分析 3. 关键价位 4. 综合研判"
```

问题：
- 没有给出具体分析步骤
- 没有教 LLM 如何处理矛盾信号
- 没有异常情况处理指南
- 输出格式约束不够强

### 5.2 优化后的 Prompt 结构

```python
TECHNICAL_SYSTEM_PROMPT_V2 = """你是专业的技术分析师。

## 你的分析步骤（按顺序执行）

### Step 1: 趋势定级
先判断当前处于什么趋势级别：
- 强上涨: 均线多头排列 + 价格在MA20上方 + MACD在零轴上方
- 弱上涨: MA5/10向上但MA20/60压制
- 震荡: 均线缠绕，价格在BOLL中轨附近反复
- 弱下跌: MA5/10向下但MA60支撑
- 强下跌: 均线空头排列 + 价格在MA20下方 + MACD在零轴下方

### Step 2: 动能确认
MACD/RSI/KDJ 三重验证趋势是否得到动能支持

### Step 3: 成交量验证
- 上涨放量 → 健康
- 上涨缩量 → 警惕假突破
- 下跌放量 → 恐慌抛售，加速见底
- 下跌缩量 → 阴跌，可能持续

### Step 4: 多周期确认
如果提供了周线数据，检查日线信号是否与周线一致

### Step 5: 关键价位
找出最近的支撑位和阻力位各2个

### Step 6: 综合判断
基于以上5步，给出最终判断。

## 异常情况处理

### 停牌/无数据
如果数据不足判断，直接输出 neutral + confidence=0 + reasoning说明

### 极端行情
- 连续涨跌停: 不预测幅度，confidence=0.1
- 异常放量(>5倍均值): 在risks中特别标注

### 矛盾信号
- 趋势看涨 + 动能看跌 → 趋势优先但降低confidence
- 多周期矛盾(日线涨+周线跌) → 短期跟随日线，但标注风险

## 输出JSON格式
{
  "direction": "bullish|bearish|neutral",
  "magnitude": {"min_pct": -5.0, "max_pct": 5.0},
  "confidence": 0.0~1.0,
  "reasoning": "按Step1-6的顺序组织",
  "key_factors": [],
  "risks": [],
  "trend_level": "strong_up|weak_up|ranging|weak_down|strong_down",
  "support_levels": [价格1, 价格2],
  "resistance_levels": [价格1, 价格2]
}

## 幅度估算参考
- 震荡市: ±1-3%
- 弱趋势: ±3-5%
- 强趋势: ±5-10%
- ATR值 = X → 日波动预期约X点，周波动约X*√5点
"""
```

### 5.3 Few-Shot 示例（可选，更高阶）

在 Prompt 末尾加 1-2 个分析示例，提高输出格式一致性：

```python
EXAMPLE_ANALYSIS = """
### 示例分析（仅作格式参考）

输入: MA空头排列，MACD死叉第3天，RSI=35，BOLL中轨向下，缩量

Step 1 趋势定级: 弱下跌（空头排列但RSI未超卖，下跌动能有限）
Step 2 动能确认: MACD死叉+RSI偏弱 → 动能支持下跌
Step 3 成交量: 缩量下跌 → 阴跌格局，非恐慌
Step 4 多周期: 日线空头，周线MA20走平 → 中期震荡偏弱
Step 5 关键价位: 支撑9.99(前低), 9.50(BOLL下轨); 阻力10.38(MA10), 10.61(MA20)
Step 6 综合: 偏空震荡，幅度-3%~+1%，置信度58%

{"direction":"bearish","magnitude":{"min_pct":-3.0,"max_pct":1.0},"confidence":0.58,...}
"""
```

---

## 6. 第四层：质量校准

### 6.1 置信度校准

**问题**：LLM 输出的 confidence 是主观的，不同 LLM、不同时间可能给同一形态不同置信度。

**方案**：利用 PredictionStore 中的历史验证数据，为置信度提供客观校准参考。

```python
class ConfidenceCalibrator:
    """置信度校准器"""
    
    def calibrate(self, agent_result: AnalysisResult) -> float:
        """
        基于该 Agent 的历史准确率，校准置信度
        
        例如：
        - Agent 说 confidence=0.68
        - 历史上该 Agent 在短期预测中，说 0.6-0.7 置信度时实际准确率 62%
        - 校准后: confidence = 0.68 * (0.62/0.65) ≈ 0.65
        """
        ...
    
    def get_historical_bias(self, agent_name: str, timeframe: str) -> dict:
        """
        分析该 Agent 的系统性偏差
        
        Returns:
            {
                "overall_accuracy": 0.62,
                "bullish_accuracy": 0.58,  # 看涨时更不准？
                "bearish_accuracy": 0.67,  # 看跌时比较准
                "overconfidence_ratio": 1.2,  # confidence平均比实际高20%
            }
        """
```

### 6.2 幅度校准

```python
class MagnitudeCalibrator:
    """幅度校准"""
    
    def get_atr_based_range(self, price_data, timeframe: str) -> tuple:
        """
        基于ATR给出该时间维度的合理波动范围
        
        短期(1周): ATR * √5 * (0.5 ~ 1.5)
        中期(1月): ATR * √20 * (0.5 ~ 1.5)
        """
        ...
    
    def validate_magnitude(self, predicted_range, atr_range) -> bool:
        """
        检查LLM预测的幅度是否在ATR合理范围内
        偏离过大 → 在reasoning中标注或调整
        """
        ...
```

### 6.3 多空偏好检测

```python
def detect_directional_bias(self, agent_name) -> str:
    """
    检测Agent是否有系统性多空偏好
    
    - 过去50次预测，看涨40次但实际只有20次涨 → 偏乐观
    - 过去50次预测，看跌10次但实际有20次跌 → 不敢看跌
    """
    stats = self.store.get_accuracy_stats(agent_name)
    # ...
```

---

## 7. 第五层：自进化机制

### 7.1 预测事后分析

每次预测被验证后，自动分析这次预测的对错原因：

```python
class PostMortemAnalyzer:
    """预测事后分析器"""
    
    async def analyze(self, prediction: PredictionRecord) -> str:
        """
        让 LLM 回顾一次预测，分析：
        1. 如果对了，哪些信号是有效的？
        2. 如果错了，忽略了什么信号？
        3. 下次类似情况应该注意什么？
        
        这些"经验"存入 PredictionStore，供 RAG 检索
        """
        prompt = f"""
        回顾一次技术分析预测：
        
        当时数据: {当时的K线/指标摘要}
        预测: {direction} {magnitude} (confidence={confidence})
        实际: {actual_direction} {actual_change_pct}%
        结果: {"✓ 正确" if correct else "✗ 错误"}
        
        请分析：
        1. 预测正确/错误的主要原因
        2. 有什么技术信号被忽略或高估了？
        3. 一条关键经验教训（一句话）
        """
        ...
```

### 7.2 标的个性化参数

不同股票的技术特征不同：
- 银行股（低波动）→ ATR 小，震荡居多
- 科技股（高波动）→ ATR 大，趋势性强
- 妖股 → 技术分析基本无效

```python
class StockProfileManager:
    """标的个性化配置"""
    
    def get_profile(self, symbol: str) -> dict:
        """
        基于历史数据，分析该标的的技术特征
        
        Returns:
            {
                "avg_atr": 0.15,        # 平均真实波幅
                "trend_following_score": 0.7,  # 趋势跟随性（高=follow趋势）
                "reversal_tendency": 0.3,      # 反转倾向
                "ma_crossover_accuracy": 0.55,  # 金叉/死叉信号准确率
                "best_indicators": ["MA20", "MACD", "VOL"],  # 对该标的最有效的指标
                "worst_indicators": ["RSI"],     # 对该标的无效的指标
            }
        """
```

这些 profiles 会传给 LLM，让它在分析时知道：
- "这个标的趋势性强，金叉信号可信度高"
- "这个标的经常假突破，突破信号要打折"

### 7.3 指标有效性追踪

```python
class IndicatorTracker:
    """追踪每个技术指标的历史预测效果"""
    
    def track(self, prediction_id, indicators_used, was_correct):
        """
        记录: 这次预测中金叉信号出现了 → 方向对了/错了
        
        积累后可以回答：
        - "MA金叉在这个标的上准确率58%"
        - "RSI超卖在这个标的上准确率62%"
        - "MACD是这个标的最可靠的指标"
        """
```

---

## 8. 实施顺序建议

```
优先级  优化项                          预计收益    难度
────── ──────────────────────────────── ────────  ────
  ⭐⭐⭐  Prompt 精炼 (第5节)             中高       低
  ⭐⭐⭐  新增指标 (ATR/OBV/ADX)         高         低
  ⭐⭐⭐  两阶段分析流程                  高         中
  ⭐⭐    多周期数据 (周线背景)           中         中
  ⭐⭐    置信度校准                      中         中
  ⭐⭐    异常情况处理模板                中         低
  ⭐      板块指数数据                    中         中
  ⭐      指标共振评分                    中         中
  ⭐      历史形态匹配 (RAG)             中         高
  ⭐      预测事后分析(自进化)           高(长期)   高
  ⭐      标的个性化参数                  高(长期)   高

建议路线:
  Day 1-2:  Prompt 精炼 + 新增指标 → 快速提升基线
  Day 3-4:  两阶段分析流程 + 多周期数据
  Day 5-6:  置信度校准 + 异常处理
  Day 7+:   自进化机制（需要预测数据积累，非一日之功）
```

---

> 📌 **核心思路**：近期股价分析师是团队中短期权重最高的 Agent，它的优化直接提升整个系统。改善不是推翻重来，而是在现有框架内逐层叠加——先让数据更丰富、再让思考更深入、最后让它从错误中学习。
