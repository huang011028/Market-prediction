# Market Prediction 总目标路线图

版本：V1 后续路线

更新日期：2026-07-16

状态：执行中

## 1. 路线原则

后续开发统一遵守以下顺序：

```text
明确可测目标
-> 冻结数据与实验协议
-> development 上发现候选
-> 独立样本验证
-> 成本与风险评价
-> 一次性 lockbox
-> 前瞻纸面验证
-> 人工批准晋升
```

不再以“新增了多少 Agent、Prompt、特征或页面”作为阶段完成标准。每项改动必须回答：改善了哪个样本外指标，是否产生经济增量，是否可复现，失败时如何回滚。

## 2. 当前基线

- 分支：`v1`；
- 正式 Quant 数据：A 股 `5d`，31,948 条 `quant_features.v4` PIT 样本，222 只股票，257 个日期；
- PIT enrichment：25,992 条因子事件、378 只股票，覆盖现金流、资产负债、历史市值和机构一致预期；
- 最新实验：Phase 2 两阶段 Quant V2，7 折、7 个特征组合，全部为 `shadow_only`；
- lockbox：4,134 条、26 个日期，保持 locked；
- 实时预测：34 条，23 条已验证；其中 33 条为 `legacy-v2`，1 条真实 V3.1 等待到期；
- Skill Registry：13 条启用规则，全部属于技术面；
- 学习型 Aggregator：已实现但未激活；
- HK/US、`20d/60d` 严格 Quant：未完成。

## 3. Phase 0：研究治理与 V3.1 前瞻队列

优先级：P0

状态：工程实现完成，真实前瞻运行验收进行中。

目标：先保证未来所有结果可信，避免在有缺陷的实验协议上继续堆模型。

### 工作项

1. [完成] 建立仓库级不可变 `experiment_ledger`，并导入历史试验。
2. [完成] 修复跨目录 `prior_trials`，同研究问题累计多重试验惩罚。
3. [完成] 冻结唯一生产候选阈值，诊断阈值禁止晋升。
4. [完成] 加入 moving block bootstrap、独立日期、选股次数和稀疏度门禁。
5. [完成] 加入市场、波动、流动性状态报告与利润集中度门禁。
6. [完成] V3.1 cohort 谱系、代码/Prompt/Skill/LLM/证据版本持久化。
7. [完成] SQLite 任务恢复、到期验证计划和指数退避重试；生产级 SLA 告警留待 Phase 5。

### 验收标准

- 新实验能正确看到此前同研究问题的试验次数；
- 相同数据与配置可复现相同 fold、模型和指标；
- 阈值不再根据 test 结果人工选择；
- 新增预测全部标记 `target_version=v3.1`；
- 任一 V3.1 预测可从输入快照追溯到最终验证结果；
- 连续服务重启不会丢失任务和到期验证计划。

## 4. Phase 1：重构 Quant 预测问题

优先级：P0

状态：工程实现与 development 验证完成，统计和经济门禁未通过，保持 Shadow。

目标：解决三分类多数类主导、Brier 输给经验先验和可操作信号过稀的问题。

### 工作项

1. [完成] 建立两阶段模型：第一阶段预测“是否存在可操作边际”，第二阶段只对候选样本预测收益和横截面排序。
2. [完成] 将收益排序目标与三分类概率分离，分别评价 Rank IC、Top-K return、Brier 和 calibration。
3. [完成] 增加稳健横截面标准化、行业中性及波动/流动性/严格 PIT 市值暴露控制。
4. [完成] 比较 Ridge、Logistic、LightGBM ranking/regression，不引入深度模型直到简单基线被稳定击败。
5. [完成] 使用 validation-only identity/sigmoid/isotonic 候选校准器，并设置最小有效样本。
6. [完成] 使用 validation split conformal 修正收益区间，并评价宽度和覆盖率。
7. [完成] 冻结 `config/quant/two_stage_v1.json`，接入全局实验账本、前端一键运行和预注册成本后组合回测。

### Development 结论

- 正式实验：`phase1_two_stage_v1b_20260716`；
- 31,946 条样本，7 折，13,564 条 OOF 测试预测，最终 4,134 条 lockbox 未读取；
- 最佳概率候选：Logistic gate + LightGBM regression + technical；
- Brier `0.170377`，折内经验先验 `0.171348`，改善不足预注册门槛；
- Gate Brier 改善 `0.001516`，95% block bootstrap CI `[-0.000247, 0.003466]`；
- Rank IC `0.033743`，CI `[0.016402, 0.050622]`，排序信号为正；
- Top-K 平均收益 `-0.325707%`，CI `[-1.515506%, 0.769065%]`；
- 12 个成本后组合均未通过完整门禁，Lockbox 继续保持 locked。

### 验收标准

- Brier 相对经验先验达到预注册改善，且 bootstrap 置信区间支持该改善；
- Rank IC 置信区间下界大于 0；
- 可操作覆盖不低于 1%，并拥有足够独立调仓日期；
- Top-K 扣成本收益不是由少数日期或单一行业贡献；
- 简单基线、两阶段模型和完整模型的增量可通过同折消融解释。

## 5. Phase 2：提高真正有信息量的 PIT 特征

优先级：P1

状态：数据工程、质量审计与 development OOF 消融已完成；统计与经济门禁未通过，保持 Shadow。

目标：从“字段覆盖高”转向“样本外增量高”。

### 工作项

1. [完成] 引入机构一致预期、盈利预测修正和年度正式值相对事前预期的 surprise；保留来源时间与版本。
2. [完成] 增加现金流质量、应计项、资本开支和资产负债表质量。
3. [完成] 增加历史市值、Beta、波动、流动性和风格暴露，形成更专业的风险残差。
4. [完成] 将公告从标题计数升级为事件类型、金额、严重度、首次披露和衰减强度。
5. [完成] 加入行业收益离散度、流动性拥挤和市值/现金流/一致预期同业排名；历史行业成员继续沿用严格有效时点连接。
6. [完成] 为每个特征族建立覆盖率、陈旧度、漂移、异常值、血缘和 OOF 增量报告。

### Development 结论

- 正式数据：`phase2_v24_20260716`，31,948 条样本，PIT 血缘违规为 0；
- 资产负债、现金流和历史市值覆盖约 `99.77%`，一致预期覆盖 `41.09%`；
- 漂移超限的业绩快报收入增速已从模型配置排除，原始字段仍保留供审计；
- 最佳 `technical_industry_v2` 的 Brier 为 `0.169450`，相对经验先验改善 `0.001896`，未达到 `0.002`；
- 相对纯技术，它通过特征增量门禁，但 Rank IC 增量为负，增量主要来自 Brier 和较少负的 Top-K 收益；
- Top-K 平均收益仍为 `-0.275250%`，七个成本后组合全部未通过；最终 lockbox 继续 locked。

### 验收标准

- 每个新特征都有 PIT 来源、有效时间、缺失语义和数据质量报告；
- 特征族只有在多个 fold 提供稳定增量时才进入候选组合；
- 完整特征包不再默认优先，负协同特征自动保持独立专家或被移除；
- 数据覆盖提高不能以当前值伪装历史值。

### Phase 2 后续收口

1. 扩大机构一致预期历史覆盖和预测修正的时间序列深度，显式建模“无覆盖”状态；
2. 提升正向 Top-K 排序，分别评价多头和负向信号，避免 A 股禁做空使强负向预测无法形成组合；
3. 对行业相对特征做状态条件化和更严格稳定性选择，禁止直接启用完整特征包；
4. 在上述增量重新通过 development 门禁前，不解锁最终 lockbox。

## 6. Phase 3：五 Agent 同口径历史 OOF 与学习型 Aggregator

优先级：P1

目标：证明每个 Agent 在哪些场景有边际价值，并让 Aggregator 学习而不是固定投票。

### 工作项

1. 在同一 PIT 样本上冻结 LLM、Prompt、Skill 和数据源版本，执行五 Agent shadow replay。
2. 保存每个 Agent 的收益分布、概率、证据质量、缺失模式、延迟和成本。
3. 按市场、周期、市场状态、行业和数据质量评价单 Agent OOF 能力。
4. 计算 Agent 错误相关性、重复证据和条件边际贡献。
5. 用非负正则化 stacking 或门控模型训练 LearnedAggregator；LLM Aggregator 继续负责解释与证据审计。
6. 将静态权重作为必须击败的基线，并保留缺失时回退策略。

### 验收标准

- 五个 Agent 在相同 fold、相同 `as_of` 上都有可审计 OOF 结果；
- LearnedAggregator 在多折上稳定优于静态 Aggregator；
- 权重能解释市场状态和缺失模式，不依赖单一 Agent；
- 同一 LLM 导致的相关错误被显式建模；
- 未通过门禁时生产静态 Aggregator 不发生改变。

## 7. Phase 4：Agent 改进工程师 V2

优先级：P1

目标：从“能生成候选”升级为“能可靠发现、验证、灰度和回滚改进”。

### 工作项

1. 基于足够大的失败场景簇生成候选，不允许针对单条或少量个案修改 Prompt/Skill。
2. 候选生成、训练、validation、holdout 和永久 Prompt lockbox 完全隔离。
3. 记录候选实际改变了哪些预测；没有行为变化的候选直接拒绝。
4. 对每条 Skill 做独立增量、冲突检测、适用范围和过期检查。
5. 新 Skill 先进入 shadow/canary，达到前瞻样本阈值后再全量启用。
6. 建立版本回滚、禁用、审计日志和一键恢复上一稳定版本。
7. 核心代码、MCP、数据源和模型接入继续只生成建议，由用户审批。

### 验收标准

- 候选至少改变预设数量的独立案例；
- holdout 与前瞻 canary 均改善目标指标，且没有显著损害其他状态；
- Registry 中每条 Skill 都能追溯训练集、验证集、版本和回滚点；
- 技术面之外至少一个 Agent 获得通过门禁的改进；
- 自动应用失败时生产 Prompt/Skill 能自动恢复。

## 8. Phase 5：多市场与多周期扩展

优先级：P2

目标：让 A/HK/US 和 `5d/20d/60d` 从接口支持变为独立验证能力。

### 工作项

1. 先冻结 A 股 `5d` 协议，再建设 A 股 `20d` 和 `60d`，避免同时改变市场和周期。
2. 为港股建设独立交易日历、复权、基准、费用、印花税、流动性和卖空约束。
3. 为美股建设独立基准、公司行动、盘前盘后、费用、借券和流动性约束。
4. 每个市场和周期拥有独立 PIT 股票池、特征版本、模型、校准器、Aggregator 和 lockbox。
5. 禁止直接把 A 股模型参数迁移为 HK/US 生产模型；迁移只能作为待验证先验。

### 验收标准

- 每个市场/周期都有独立数据卡、Walk-forward、成本模型和晋升报告；
- 不同市场之间没有标签、交易日历或未来数据混用；
- 至少一个非 A 股研究候选完成 locked development 流程；
- 多周期预测不会因重叠标签产生泄漏或虚假样本量。

## 9. Phase 6：纸面生产与运行工程

优先级：P2

目标：在不接入真实资金的情况下验证完整运行闭环。

### 工作项

1. 使用持久任务队列替代进程内 `analysis_jobs`。
2. 建立快照、预测、验证、训练和纸面订单调度器。
3. 增加数据源成功率、新鲜度、延迟、缺失、漂移和成本监控。
4. 建立模型、Prompt、Skill、LLM 和数据版本发布记录。
5. 完善 A 股停牌、涨跌停、T+1、整手、分红送转和实际现金账本。
6. 生成每日纸面持仓、订单、成交、归因、风险和异常报告。
7. 增加人工审批、kill switch、最大风险预算和回滚演练。

### 验收标准

- 服务重启后任务、预测、订单和验证计划可恢复；
- 连续前瞻期内数据和任务 SLA 达标；
- 纸面成交可对账，研究回测与纸面结果差异可解释；
- 未通过风险或数据质量门禁时自动不交易；
- 真实资金功能默认不存在或保持硬关闭。

## 10. 总目标完成门槛

项目只有满足以下条件，才可以从“研究平台”升级为“可试点决策系统”：

1. 至少一个市场和周期的冻结模型通过 development、一次性 lockbox 和前瞻纸面验证。
2. 概率、排序和成本后收益均优于预注册基线，并具有正的置信区间下界。
3. 五 Agent 或 Quant+Agent Aggregator 的边际价值能够由严格 OOF 证明。
4. 所有自动 Prompt/Skill 晋升都有独立验证、canary 和回滚。
5. 全局实验账本、数据血缘、版本与失败结果完整可审计。
6. 运行系统具备持久队列、调度、监控、数据 SLA 和风险熔断。
7. 用户明确批准后，才进入极小规模真实资金试点讨论。

## 11. 当前执行顺序

Phase 0、Phase 1 和 Phase 2 工程已经完成，当前执行顺序调整为：

1. 通过正常前端/API 预测填充首批真实 V3.1 cohort，并观察重启与到期验证；
2. 完成 Phase 2 收口：扩大一致预期覆盖、建模缺失路径，并针对正向 Top-K 收益改进排序；
3. 每个新增特征族继续先做同折消融，禁止默认拼接完整特征包；
4. 启动 Phase 3 的五 Agent 同口径 OOF shadow replay，但只有足够 PIT 证据后才训练 LearnedAggregator；
5. 继续锁定最终 lockbox，不因单个正收益点估计或临界 Brier 改善而解锁。

这条顺序的核心是先让证据可信，再追求更好的模型结果。
