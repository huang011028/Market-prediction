# Market Prediction 项目状态评审

评审日期：2026-07-16

适用分支：`v1`

评审依据：当前代码、`data/predictions.db`、Skill Registry、PIT 数据资产、Research Data V2.4、Phase 1/2 两阶段 Quant 固定 Walk-forward、特征质量审计、全局实验账本、组合稳定性门禁和离线测试。

## 1. 总目标

项目的总目标不是让五个 Agent 给出一份更长的股票分析，而是建立一个可审计、可验证、可持续改进的股票决策研究系统：

1. 面向 A 股、港股和美股，由五个专业 Agent 生成相互独立、带证据质量的判断。
2. 预测未来 `5d / 20d / 60d` 风险调整后超额收益分布，而不是模糊的看涨、看跌或中性。
3. Aggregator 根据样本外能力、证据质量、市场状态和错误相关性判断是否存在可操作边际。
4. 所有模型、Prompt 和 Skill 必须通过 PIT 回放、Walk-forward、独立验证和成本后组合评价后才能晋升。
5. Agent 改进工程师能够从真实失败场景生成候选 Prompt/Skill，但不能绕过验证门禁，也不能自动修改核心代码、MCP 或数据源。
6. 在真实资金使用前，系统必须证明跨时期、跨市场状态、扣除成本后的稳定经济价值，并保留人工审批、回滚和审计能力。

## 2. 总体完成度

完成度不是代码文件数量，而是按“实现、验证、生产证明”三层评价。

| 目标层级 | 当前完成度 | 判断 |
| --- | ---: | --- |
| 研究平台工程能力 | 约 90% | 两阶段预测、PIT 因子事件、特征审计、全局研究治理和持久任务状态已实现，能够构建、回放、消融并拒绝弱候选 |
| 可验证的预测能力 | 约 50% | A 股 `5d` 的 Rank IC 置信区间为正，Phase 2 行业族有小幅 OOF 增量，但概率和成本后收益门禁仍未通过 |
| 安全自动进化能力 | 约 35% | 候选生成、Replay 和 Registry 已有，只有技术面少量规则得到有限验证 |
| 多市场生产决策能力 | 约 20% | 线上分析可运行，但 HK/US、`20d/60d` 和真实前瞻证据不足 |
| 总目标综合完成度 | 约 58% | Phase 2 数据与验证闭环已完成，但预测有效性、Agent OOF 和跨市场前瞻证明仍未完成 |

这里的 58% 不是预测准确率。它表示总目标所需的工程、数据、科学验证和运行治理略过一半。项目已经是成熟的研究平台，但不能称为生产级预测或自动交易系统。

## 3. 分方面完成情况

| 方面 | 完成度 | 已完成 | 主要距离 |
| --- | ---: | --- | --- |
| 前端与交互产品 | 80% | 多市场输入、五 Agent 进度、Target V3 决策卡、K 线、历史记录、模型配置、调优与 Quant 页面；任务进度已持久化 | 仍缺多进程队列、任务并发限制、告警和运行监控 |
| 五 Agent 实时分析 | 70% | 技术、新闻、基本面、宏观、行业均可运行，具备降级状态和数据质量摘要 | 外部数据仍会缺失；同一 LLM 导致错误相关；四个非技术 Agent 的历史预测能力未被证明 |
| Target V3.1 契约 | 80% | 固定交易日 horizon、滚动 Beta 市场残差、概率、分位数、edge、不交易原因和完整版本谱系已进入代码与 schema；首条真实 V3.1 已落盘 | 新 cohort 尚无到期样本，运行验证仍需时间积累 |
| 预测持久化与到期验证 | 70% | V3 字段、真实收益、Brier、edge hit、验证重试和 cohort 状态已实现 | 当前仅 23 条已验证记录且均为旧口径；需要真实运行积累 V3.1 样本 |
| A 股 PIT 研究数据 | 86% | `quant_features.v4` 31,948 条、222 只股票、257 个日期；25,992 条严格有效时点因子事件覆盖 378 只股票；市值/现金流/资产负债覆盖约 99.77% | 一致预期仅覆盖 41.09%，历史深度和股票池仍有限；更严格复权/交易日历仍需完善 |
| HK/US 与多周期 Quant | 20% | 实时分析路径支持 A/HK/US，目标定义支持 `5d/20d/60d` | 正式 Quant 数据、Walk-forward 和组合证据目前只有 A 股 `5d` |
| 统计模型与概率校准 | 74% | 一阶段基线和两阶段 Gate+Rank 均已实现；新增特征族同折 OOF 增量门禁、稳健截面标准化和 validation-only 校准 | 最佳 Phase 2 Brier 改善仍差 `0.000104` 才达到门槛，Top-K 收益为负，没有生产模型 |
| 组合与经济评价 | 76% | Phase 2 七个特征族均自动进入预注册成本后回测，含严格 PIT 市值、容量、block bootstrap、稀疏度、集中度和状态门禁 | A 股禁做空条件下高 edge 正向信号极少，全部组合未通过；更细成交仿真和一次性 lockbox 仍未执行 |
| 学习型 Aggregator | 30% | OOF 输入、非负权重、purge、缺失模式和晋升门禁代码已实现 | 五 Agent 同时点 OOF 样本不足，生产仍使用静态权重与质量折扣 |
| Agent 改进工程师 | 40% | 历史评估、LLM 候选、Prompt Replay、holdout、权限隔离和 Skill Registry 已实现 | 13 条启用 Skill 全部属于技术面；样本规模偏小；三轮 LLM Prompt Replay 均未改变预测 |
| 工程质量与治理 | 78% | 离线测试、manifest、PIT 血缘、全局只追加实验账本、预注册策略和 SQLite 持久任务 | 仍缺 CI、分布式队列、服务监控、数据 SLA 与密钥部署治理 |

## 4. 当前可确认的研究结果

### 4.1 Research Data V2.3

正式实验：`output/research_data_v2/20260715_phase0_v23/`

- A 股，`5d`，2021-01-04 至 2025-12-01；
- 31,946 条 `quant_features.v3`；
- 221 只有效股票，257 个周频截面；
- 27,812 条 development 样本；
- 4,134 条、26 个日期的 lockbox，仍为 `locked`；
- 7 折 purged Walk-forward，每个候选 13,564 条 OOF 测试预测；
- 数据 hash：`854de3f23967bc170422`。

PIT 特征覆盖：技术 100%、基本面 99.81%、高质量基本面 97.57%、业绩事件 94.26%、正式公告 99.25%、行业 92.80%、估值 86.29%、可计算 surprise 21.12%。

### 4.2 预测能力结论

最低 Brier 候选 `lightgbm__technical_fundamental`：

- Brier `0.177851`，差于经验先验 `0.170240`；
- Rank IC `0.068883`，95% 下界 `-0.018585`；
- 可操作覆盖 `0.0902%`；
- 方向收益置信区间下界为负。

纯技术 LightGBM 的 Rank IC 为 `0.078606`，95% 下界 `0.017947`，说明存在弱但可重复的横截面排序信息；然而可操作覆盖只有 `0.1546%`，概率质量仍未击败经验先验。

完整 `research_v2` 将可操作覆盖提高到 `1.1714%`，但 Rank IC 降至 `0.053846`，95% 下界为负，说明更多特征没有自动转化为更多有效信息。

最终晋升结果：`should_promote=false`、`shadow_only=true`。这是一项正确结果，不应通过降低门槛来改变。

全局实验账本已导入 7 次历史试验，本轮是同一研究问题的第 8 次。多重试验惩罚因子为 `1.780428`，要求 Brier 改善从基础 `0.002` 提高到 `0.003561`；本轮实际 Brier 增量为 `-0.007611`，未通过。

### 4.3 Phase 1 两阶段 Quant

正式实验：`output/quant_two_stage/phase1_two_stage_v1b_20260716/`

- Gate：Logistic / LightGBM；排序：Ridge / LightGBM regression / LambdaRank；
- 2 套特征、12 个候选、7 折、每个候选 13,564 条 OOF 测试预测；
- 最佳概率候选为 `gate_logistic__rank_lightgbm_regression__technical`；
- Brier `0.170377`，折内训练先验基线 `0.171348`，改善不足预注册门槛；
- Gate Brier 改善 `0.001516`，95% block bootstrap CI `[-0.000247, 0.003466]`；
- Rank IC `0.033743`，95% CI `[0.016402, 0.050622]`，说明弱排序信号可重复；
- 可操作覆盖 `9.40%`，但 Top-K 平均收益 `-0.325707%`，CI 跨 0；
- 区间覆盖率 `78.35%`，接近预设 80%；行业集中度通过；
- 现有 PIT 数据没有历史总股本/市值，规模暴露门禁未通过；
- 12 个预注册成本后组合均未通过完整稳定性门禁。

结论：两阶段建模解决了“一个三分类模型承担所有目标”的结构问题，但没有创造足够强的新信息。Rank IC 为正不等于概率可靠或可交易，不能解锁 Lockbox。

### 4.4 Phase 2 PIT 特征与两阶段验证

正式数据：`output/research_data_v2/phase2_v24_20260716/`

正式实验：`output/quant_two_stage/phase2_two_stage_v2b_20260716/`

- `quant_features.v4` 共 31,948 条、222 只股票、257 个日期，最终 lockbox 仍未读取；
- 新增 25,992 条带 `effective_date/report_date/source_ref` 的因子事件，覆盖 378 只股票；
- 现金流、资产负债和严格 PIT 市值覆盖均约 `99.77%`，行业覆盖 `92.79%`，机构一致预期覆盖 `41.09%`；
- 特征审计检查覆盖率、陈旧度、漂移、异常值和血缘，PIT 血缘违规为 0；原始业绩快报收入增速漂移超限，已从模型特征中排除而非降低门槛；
- 七个特征组合使用同一 Logistic Gate + LightGBM Rank、同一 7 折和同一预注册门禁比较；
- 最佳候选为 `technical_industry_v2`，Brier `0.169450`，经验先验 `0.171346`，改善 `0.001896`，略低于要求的 `0.002`；
- Rank IC `0.020791`，95% 下界 `0.013419`；相对纯技术，Brier 改善 `0.000579`、Top-K 收益改善 `0.137105` 个百分点，因此通过特征族增量门禁；
- 但其 Top-K 平均残差收益仍为 `-0.275250%`，95% 下界 `-1.404198%`；完整 Phase 2 特征包表现更差，证明全量拼接不是正确方向；
- 严格 PIT 市值暴露覆盖 `99.7684%`，Phase 1 的规模门禁缺口已经解决；七个成本后组合仍全部 `shadow_only`，最终 lockbox 继续锁定。

结论：Phase 2 完成了数据与验证工程目标，并证明行业相对特征存在小幅概率增量；它没有证明可交易性。该结果应保留为下一轮特征选择基线，不能直接进入正式预测。

### 4.5 组合结果的正确解释

在 development OOF 上，`edge_score >= 0.10` 的技术及技术+行业组合出现较高成本后收益：

| 模型 | 成本后收益 | 复合超额 | Sharpe | 最大回撤 | 平均持仓 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 技术 | +58.12% | +30.70% | 1.215 | -7.68% | 0.748 |
| 技术+行业 | +66.18% | +37.61% | 1.141 | -7.68% | 0.804 |
| 完整 Research V2 | -1.43% | -17.99% | 0.103 | -29.56% | 0.860 |

V2.3 已对预注册 `edge_score >= 0.10` 执行 1,000 次、4 日 block bootstrap。技术+行业虽然复合超额为 `+37.61%`，但技术策略的超额置信区间为 `[-48.34%, +168.05%]`，只有 23 个实际持仓日、80 次选股，前五个盈利日贡献 `89.39%`，最差状态超额低于 `-45%`。因此所有组合继续 `shadow_only`。这证明此前的漂亮收益确实不能作为可交易证据。

## 5. 当前最严重的问题

### P0：两阶段模型仍未形成可靠概率和经济收益

结构拆分、严格规模和逐族消融已经完成，但最佳候选的概率改善仍差 `0.000104` 才达到预注册门槛，Top-K 收益为负，成本后组合也没有通过。当前主要矛盾已从“数据字段缺失”转为“一致预期覆盖不足、正向排序能力弱、特征在不同市场状态下不稳定”。

### 已解决的 P0：严格 PIT 规模与风格暴露

Phase 2 已用报告时点股本和历史价格构造严格 PIT 市值、账面市值比和规模排名，模型中的规模暴露覆盖达到 `99.7684%`。缺失仍保持缺失，没有用成交额或当前市值伪装历史规模。

### P0：机构一致预期覆盖与时序深度不足

一致预期只覆盖 `41.09%`，修正排名能形成的有效行业截面更少。当前数据可以验证采集和 PIT 边界，却不足以让完整特征包稳定学习。下一步必须扩展券商报告历史、预测期映射和缺失模式建模，并单独评价“有一致预期覆盖”和“无覆盖”两类样本，不能用零填充把缺失误当成中性观点。

### P0：线上 V3.1 证据尚未真正开始积累

当前 `predictions.db` 有 34 条预测：33 条 `legacy-v2`，以及首条真实 V3.1 前瞻预测 `bbe5fe8e`。该记录完整保存 cohort、代码/Prompt/Skill/LLM 与五 Agent 证据谱系，并计划在 2026-07-22 后验证。剩余差距是形成足量到期样本，而不是继续证明字段能否落盘。

### 已解决的 P0：多重试验账本与阈值边界

仓库级 SQLite 账本已导入 7 次历史试验，本轮正确读取 `prior_trials=7`，执行后全局累计为 8。生产候选只允许使用配置中预注册的 `edge=0.10`；`edge=0.02` 明确标记为 diagnostic，不具备晋升资格。账本表通过数据库触发器禁止更新和删除。

### P0：正收益组合仍可能是稀疏样本和阈值选择的结果

预注册组合已经接受日期 block bootstrap、独立日期/持仓日/选股次数、利润集中度及牛熊震荡、波动和流动性状态门禁，并明确未通过。现在的剩余问题不再是“没有检查”，而是信号本身太稀疏、置信区间过宽、状态失效严重；最终 lockbox 继续保持锁定。

### P1：五 Agent 仍未形成同口径 OOF 训练集

Quant 特征消融不等于历史上五个 LLM Agent 的真实输出。没有同一 `as_of`、同一模型版本、同一 Prompt/Skill 版本、同一证据快照下的五 Agent OOF 概率，就无法可靠学习 Aggregator 权重，也无法判断多个 Agent 是否只是重复同一错误。

### P1：自进化工程闭环存在，科学闭环仍很弱

Skill Registry 有 13 条启用规则，全部来自技术面；方向 Skill 的 holdout 为 66 条，置信度 Skill 为 84 条。三轮 LLM Prompt Replay 各只有 12 条样本，候选没有改变任何预测。当前系统能够管理候选，但尚未证明能稳定发现并晋升有价值的新 Prompt/Skill。

### P1：跨市场、跨周期目标只完成了接口，没有完成验证

实时系统支持 A/HK/US，Target V3.1 定义了 `5d/20d/60d`，但严格 PIT Quant、Walk-forward、组合成本和 Agent OOF 目前只有 A 股 `5d`。不能把接口支持理解为能力支持。

### P2：运行可靠性仍未达到生产调度水平

分析任务请求、进度、结果和恢复次数已写入 `data/runtime_jobs.db`，重启后会按重试预算重新排队；预测验证也具备指数退避重试。它仍是单机 SQLite 调度，不支持多进程抢占、分布式锁、统一 SLA、熔断统计和持续告警。

## 6. 需要进行的反思

1. **先完成模块不等于先解决目标。** 项目早期花了大量精力让每个 Agent“能说”，但预测目标、标签和验证后来才统一。今后任何新功能必须先说明它改善哪个可测指标。
2. **覆盖率不等于信息量。** 基本面 99% 覆盖、公告 99% 覆盖很有价值，但完整特征包反而更差。数据工程应以增量 OOF 贡献验收，而不是字段填满率验收。
3. **校准不能创造预测能力。** Temperature scaling 可以降低 ECE，却不能把无信息模型变成有信息模型。模型选择应先看信息增量，再看校准。
4. **漂亮回测最需要怀疑。** 稀疏策略、反复观察阈值、同一 development 集上的多轮试验，都会产生看似优秀的收益。任何高收益结论都必须经过冻结协议和一次性 lockbox。
5. **五个 Agent 不是五张独立选票。** 它们共享 LLM、上下文和部分数据源，错误高度相关。Aggregator 应学习边际贡献和错误相关，而不是只学习平均权重。
6. **自动生成候选不等于自动进化。** 真正的进化发生在候选通过独立样本并安全晋升时。没有足够样本、没有改变预测或只提高训练集指标的候选都应保持草案。
7. **研究资产必须跨实验治理。** 数据 hash、配置 hash 和单次 manifest 已有，但试验次数、阈值选择和失败结果必须进入全局不可变账本，才能控制回测过拟合。

## 7. 达到总目标的改进方向

详细执行计划见 `PROJECT_ROADMAP.md`。优先顺序如下：

1. 用真实前端/API 分析持续填充 V3.1 前瞻 cohort，并观察重启恢复和到期重试的运行表现。
2. 扩大严格 PIT 一致预期历史和股票池，建立覆盖/缺失双路径，并增强预测修正、分歧和 surprise 的时序特征。
3. 以纯技术为基线，继续做行业相对、现金流和一致预期的同折增量消融；优先优化 Top-K 正向收益，而不是继续增加完整特征包。
4. 让五 Agent 在同一 PIT 历史样本上运行 shadow replay，形成可用于学习型 Aggregator 的严格 OOF 数据。
5. 只保留有独立增量的基本面、新闻、行业和宏观专家模型，禁止无约束全量拼接。
6. 在 A 股 `5d` 冻结研究协议后，再分别建设 A 股 `20d/60d`、港股和美股管线。
7. 将 Agent 改进工程升级为全局版本、回滚、canary 和永久 Prompt lockbox；核心代码、MCP 与数据源继续人工审批。
8. 最后把当前 SQLite 持久任务升级为纸面交易调度、监控和数据 SLA。生产工程不能替代预测证明，但在真实前瞻验证前必须具备。

## 8. 生产晋级的最低验收标准

一个候选只有同时满足以下条件才允许从 `shadow` 进入纸面生产：

- 在预注册的多折 Walk-forward 上，Brier 明显优于经验先验，且改善达到预设阈值；
- Rank IC 和成本后方向收益的置信区间下界均大于 0；
- 可操作覆盖不低于 1%，同时具有足够独立调仓日期和交易数；
- 日期 block bootstrap 后的净超额收益置信区间下界大于 0；
- 牛、熊、震荡、高波动和流动性收缩状态下不存在灾难性失效；
- 交易成本、容量、停牌、涨跌停、T+1、整手和公司行动已纳入；
- 全局试验账本记录所有候选、阈值和失败结果；
- 模型、数据、Prompt、Skill、LLM 和配置版本全部冻结；
- 最终 lockbox 只解锁一次，未通过则开启新研究周期而不是继续查看同一 lockbox；
- 纸面前瞻期继续通过后，才允许讨论真实资金试点。

## 9. 最终判断

V1 已经完成了一个高质量研究平台最难搭建的骨架：目标契约、PIT 血缘、历史数据、样本外验证、候选拒绝、成本回测和权限隔离。当前系统最大的价值是能够诚实地说“这个模型还不够好”。

距离总目标最远的不是前端，也不是某个 Agent 的 Prompt，而是三件事：

1. 获得真正优于简单先验的稳定预测信息；
2. 用同口径 OOF 数据证明五 Agent 和 Aggregator 的边际价值；
3. 用冻结协议和真实前瞻结果证明扣除成本后的经济价值。

在这三件事完成前，项目应继续定位为研究与纸面决策辅助系统，而不是可靠收益模型。
